/**
 * Serverless type definitions (Phase 4 P0 - Serverless Module)
 * Re-exports from API service for use in components
 */
export type {
  ServerlessFunction,
  ServerlessTrigger,
  ServerlessDeployment,
  ServerlessMetrics,
  ServerlessLog,
  AutoScalingRecommendation,
  AggregateMetrics,
  FunctionStatus,
  TriggerType,
  FunctionRuntime,
} from '@/api/serverless';

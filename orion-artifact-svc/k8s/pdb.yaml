apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orion-artifact-pdb
  namespace: orion
  labels:
    app: orion-artifact
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: orion-artifact

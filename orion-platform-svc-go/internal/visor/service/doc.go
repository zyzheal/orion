// Package service defines the visor service layer.
// Note: This package is intentionally minimal — the actual service
// implementation lives in internal/visor/internal/service/.
// The handler currently imports this package but is not wired in the server.
package service

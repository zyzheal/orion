# 使用 Debian-slim (glibc) 替代 Alpine，兼容 Rocky Linux 8+ / RHEL / CentOS
FROM node:20-slim

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source
COPY . .

# Expose port
EXPOSE 3001

# Start development server
CMD ["npm", "run", "dev"]
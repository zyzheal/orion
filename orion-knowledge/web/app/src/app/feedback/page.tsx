import Feedback from '@/views/feedback';
import { Box } from '@mui/material';

const FeedbackPage = () => {
  return (
    <Box
      sx={{
        width: '100vw',
        height: '100vh',
      }}
    >
      <Feedback />
    </Box>
  );
};

export default FeedbackPage;

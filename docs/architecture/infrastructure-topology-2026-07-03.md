# 基础设施拓扑图（2026-07-03 自动生成）

> **生成方式**: 基于 `docs/architecture/service-authority-registry.md` + `docs/architecture/当前系统架构.md` 自动生成
> **对应任务**: Phase 2.35

---

## 一、当前部署拓扑（单体模式）

```
                            ┌─────────────────────────────────────────┐
                            │          Load Balancer / Ingress          │
                            │              (nginx / k8s)                │
                            └─────────────────┬───────────────────────┘
                                              │
                        ┌─────────────────────┼─────────────────────┐
                        │                     │                     │
                        ▼                     ▼                     ▼
            ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐
            │ orion-api-gateway │  │   orion-frontend  │  │    orion-dba      │
            │   Fastify Proxy   │  │   Vite Dev Server │  │   Vue3 + AntDV    │
            │    端口: 3000     │  │    端口: 5173     │  │    端口: 3001     │
            │   healthz: 3000   │  │                   │  │                   │
            └─────────┬─────────┘  └───────────────────┘  └───────────────────┘
                      │
                      │ HTTP 代理
                      ▼
            ┌───────────────────────────────────────────────────┐
            │              orion-platform-service               │
            │              (Node.js + Fastify)                  │
            │              端口: 3001                           │
            │                                                    │
            │  ┌─────────────────────────────────────────────┐  │
            │  │  48 Route 模块 → 42 Controllers             │  │
            │  │  → 70+ Services → 38 PostgreSQL Repos       │  │
            │  │  → EventBus (NATS optional) → Redis          │  │
            │  └─────────────────────────────────────────────┘  │
            └───────────┬───────────────────┬───────────────────┘
                        │                   │
                        ▼                   ▼
            ┌───────────────────┐  ┌───────────────────┐
            │   PostgreSQL      │  │      Redis        │
            │   端口: 5432      │  │   端口: 6379      │
            │  70+ tables       │  │  Token/Cache      │
            │  643 migrations   │  │  Session/SSE      │
            └───────────────────┘  └───────────────────┘

                        │
                        │ (NATS, 可选)
                        ▼
            ┌───────────────────┐
            │   NATS Message    │
            │     Bus            │
            │   端口: 4222      │
            │  EventPub/Sub     │
            └───────────────────┘
```

---

## 二、Go 微服务迁移目标拓扑（Phase 6）

```
                            ┌─────────────────────────────────────────┐
                            │          Load Balancer / Ingress          │
                            └─────────────────┬───────────────────────┘
                                              │
              ┌─────────────────────────────────┼─────────────────────────────────┐
              │                                 │                                 │
              ▼                                 ▼                                 ▼
    ┌───────────────────┐           ┌───────────────────┐           ┌───────────────────┐
    │ orion-api-gateway │           │  orion-frontend  │           │    orion-visor     │
    │    端口: 3000     │           │    端口: 5173     │           │    端口: 3003     │
    └─────────┬─────────┘           └───────────────────┘           └───────────────────┘
              │
              │ gRPC / HTTP2
              ▼
    ┌───────────────────────────────────────────────────────────────────┐
    │                    Orion Service Mesh (istio / k8s)              │
    │                                                                   │
    │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐    │
    │  │ Auth-Svc   │ │Tenant-Svc │ │ Pipeline  │ │  Deploy   │    │
    │  │   Go 3025  │ │  Go 3026  │ │ -Svc Go   │ │ -Svc Go   │    │
    │  └────────────┘ └────────────┘ └────────────┘ └────────────┘    │
    │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐    │
    │  │  Code-Svc  │ │ Ticket-   │ │  Chatops  │ │  Self-    │    │
    │  │   Go 3010  │ │   Svc Go  │ │ -Svc Go   │ │ Healing   │    │
    │  │            │ │  3004     │ │  3027     │ │ -Svc Go   │    │
    │  └────────────┘ └────────────┘ └────────────┘ └────────────┘    │
    │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐    │
    │  │ Finops-Svc │ │ Security  │ │ Notify-   │ │  Build-   │    │
    │  │   Go 3009  │ │ -Svc Go   │ │   Svc Go  │ │   Svc Go  │    │
    │  │            │ │  3013     │ │  3019     │ │  3037     │    │
    │  └────────────┘ └────────────┘ └────────────┘ └────────────┘    │
    │                                                                   │
    │  [Wave 1: 8 Go 服务已就绪] [Wave 2: 18 Go 需补充] [Wave 3: 5 待建] │
    └──────────────────────────┬────────────────────────────────────────┘
                               │
                               ▼
                    ┌───────────────────────┐
                    │   PostgreSQL 集群     │
                    │   读写分离 + 分片     │
                    └───────────────────────┘
                    ┌───────────────────────┐
                    │   NATS Cluster        │
                    │   (事件总线)           │
                    └───────────────────────┘
                    ┌───────────────────────┐
                    │   Redis Cluster       │
                    │   (缓存/会话)          │
                    └───────────────────────┘
```

---

## 三、服务端口总表

### 当前部署（Node.js 单体）

| 服务 | 目录 | 端口 | 协议 | 状态 |
|------|------|------|------|------|
| API Gateway | `orion-api-gateway/` | 3000 | HTTP | ✅ 运行中 |
| Platform Service | `orion-platform-service/` | 3001 | HTTP | ✅ 运行中 |
| DBA | `orion-dba/` | 3001 | HTTP | ✅ 运行中 |
| Knowledge | `orion-knowledge/` | 3002 | HTTP | ✅ 运行中 |
| Visor | `orion-visor/` | 3003 | HTTP | ✅ 运行中 |
| PostgreSQL | 外部 | 5432 | TCP | ✅ 运行中 |
| Redis | 外部 | 6379 | TCP | ⚪ 可选 |
| NATS | 外部 | 4222 | TCP | ⚪ 可选 |

### Go 微服务目标部署（Wave 1 已就绪）

| 服务 | Go 目录 | 目标端口 | 状态 |
|------|---------|---------|------|
| CMDB | `orion-cmdb-svc-go/` | 3030 | ✅ 构建通过 |
| Runner | `orion-runner-svc-go/` | 3028 | ✅ 构建通过 |
| Visor-Go | `orion-visor-svc-go/` | 3034 | ✅ 构建通过 |
| Inception | `orion-inception-svc-go/` | 3031 | ✅ 构建通过 |
| Config-Mgmt | `orion-config-mgmt-svc-go/` | 3029 | ✅ 构建通过 |
| Skill | `orion-skill-svc-go/` | 3023 | ✅ 构建通过 |
| Digital-Twin | `orion-digital-twin-svc-go/` | 3008 | ✅ 构建通过 |
| Canary | `orion-canary-svc-go/` | - | ✅ 构建通过 |

### Wave 2 待补充服务

| 服务 | Go 目录 | 目标端口 | 行数差距 | 状态 |
|------|---------|---------|---------|------|
| Pipeline | `orion-pipeline-svc-go/` | 3002 | 3478 vs 26197 | 🔧 需补充 |
| Ticket | `orion-ticket-svc-go/` | 3004 | 7321 vs 13816 | 🔧 需补充 |
| Deploy | `orion-deploy-svc-go/` | 3003 | 1197 vs 6732 | 🔧 需补充 |
| Code | `orion-code-svc-go/` | 3010 | 1873 vs 13379 | 🔧 需补充 |
| Finops | `orion-finops-svc-go/` | 3009 | 2500 vs 8383 | 🔧 需补充 |
| Chatops | `orion-chatops-svc-go/` | 3027 | 2853 vs 9185 | 🔧 需补充 |
| Security | `orion-security-svc-go/` | 3013 | 1276 vs 7759 | 🔧 需补充 |
| Approval | `orion-approval-svc-go/` | 3018 | 1411 vs 2890 | 🔧 需补充 |
| Artifact | `orion-artifact-svc-go/` | 3014 | 1184 vs 3580 | 🔧 需补充 |
| Notify | `orion-notify-svc-go/` | 3019 | 1182 vs 1701 | 🔧 需补充 |
| SelfHealing | `orion-selfhealing-svc-go/` | 3024 | 1108 vs 2313 | 🔧 需补充 |
| Plugin | `orion-plugin-svc-go/` | 3011 | 950 vs 4446 | 🔧 需补充 |

---

## 四、网络拓扑（K8s 部署视图）

```
                    ┌──────────────────┐
                    │   Ingress NGINX  │
                    │   (或 API GW)    │
                    └────────┬─────────┘
                             │
                    ┌────────▼────────────────────────┐
                    │       Kubernetes Cluster         │
                    │                                   │
                    │  ┌────────────┐  ┌────────────┐  │
                    │  │ orion-     │  │ orion-     │  │
                    │  │ platform   │  │   dba      │  │
                    │  │ -service   │  │            │  │
                    │  │ Replica: 3 │  │ Replica: 1 │  │
                    │  └────────────┘  └────────────┘  │
                    │                                   │
                    │  ┌──────────────────────────┐     │
                    │  │   PostgreSQL StatefulSet │     │
                    │  │   Primary + 2 Replicas   │     │
                    │  └──────────────────────────┘     │
                    │                                   │
                    │  ┌──────────────────────────┐     │
                    │  │   Redis Cluster (3m+3s)  │     │
                    │  └──────────────────────────┘     │
                    │                                   │
                    │  ┌──────────────────────────┐     │
                    │  │   NATS Cluster (3 nodes) │     │
                    │  └──────────────────────────┘     │
                    │                                   │
                    └───────────────────────────────────┘
                             │
                    ┌────────▼─────────┐
                    │   Monitoring     │
                    │   Prometheus     │
                    │   + Grafana      │
                    └──────────────────┘
```

---

## 五、存储分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    存储分层架构                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  L1: 热数据 (Hot)                                               │
│  ├── Redis (内存)                                               │
│  │   • Token / Session / 验证码                                  │
│  │   • Pipeline 运行状态缓存                                      │
│  │   • Rate Limiter                                             │
│  └── 内存 Map (进程内)                                           │
│      • Saga 会话 (healingSessions)                              │
│      • SSE 连接映射                                              │
│                                                                 │
│  L2: 温数据 (Warm)                                              │
│  ├── PostgreSQL 主库                                             │
│  │   • 业务数据 (70+ tables)                                    │
│  │   • 近期 Pipeline 运行记录                                    │
│  └── PostgreSQL 从库                                             │
│      • 读查询分流                                                │
│                                                                 │
│  L3: 冷数据 (Cold)                                              │
│  ├── PostgreSQL 归档                                            │
│  │   • 历史 Pipeline 运行 (>90天)                               │
│  │   • 审计日志 (>1年)                                          │
│  └── 对象存储 (S3/MinIO)                                        │
│      • 构建产物                                                 │
│      • 日志归档                                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 六、关键依赖关系

```
                    ┌──────────────────┐
                    │   orion-frontend │
                    └────────┬─────────┘
                             │ HTTP/REST
                    ┌────────▼─────────┐
                    │ orion-api-gateway│
                    └────────┬─────────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────┐ ┌──────────────┐ ┌──────────────────┐
│ Platform Svc    │ │  Auth Svc    │ │  Tenant Svc     │
│ (核心单体)       │ │ (待 Go 化)   │ │ (待 Go 化)      │
└────────┬────────┘ └──────────────┘ └──────────────────┘
         │
    ┌────┴────┬──────────┬──────────┐
    │         │          │          │
    ▼         ▼          ▼          ▼
┌───────┐ ┌───────┐ ┌───────┐ ┌────────┐
│ PG    │ │ Redis │ │ NATS  │ │ S3     │
│主库   │ │缓存   │ │事件   │ │制品   │
└───────┘ └───────┘ └───────┘ └────────┘
```

---

## 七、容量规划参考

| 组件 | 当前规模 | 目标规模 (Phase 6) | 备注 |
|------|---------|-------------------|------|
| orion-platform-service | 1 实例 | 3 实例 (拆分后) | 单体 → 微服务 |
| PostgreSQL | 1 主库 | 1 主 + 2 从 | 读写分离 |
| Redis | 1 实例 | 3 节点 Cluster | 高可用 |
| NATS | 1 实例 | 3 节点 Cluster | 事件持久化 |
| K8s Pod 总数 | ~5 | ~50+ | Go 微服务全量部署 |

# Orion 平台修复执行计划

**日期**: 2026-08-02 | **基于**: `docs/10域深度统一分析报告-2026-08-02.md` | **状态**: 10 域 P0 重复模块清理已完成

---

## 总览

| 指标 | 数值 |
|------|------|
| 总任务数 | 24 项 |
| 总工作量 | ~40-65 人天 |
| 建议周期 | 6-8 周 |
| 依赖 DAG 层级 | 3 层 |
| 最高并行度 | 6 个 Agent 并行 |

---

## 依赖 DAG

```
Phase 1A (无依赖，可并行)
  ├─ 1.1 注册5个未激活路由 ──────────────────────┐
  ├─ 1.2 废弃/冻结 identity/ 模块 ────────────────┤
  ├─ 1.3 为logging补测试 ─────────────────────────┤
  ├─ 1.4 为cost-allocation加权限控制 ─────────────┤
  ├─ 1.5 为4个核心模块补测试 ─────────────────────┤
  └─ 1.6 修复finops-v2桩实现 ─────────────────────┘
                                                      │
Phase 1B (依赖 Phase 1A 部分)
  └─ 1.7 实现ABAC策略评估引擎 (依赖 identity 废弃) ──┘
                                                      │
Phase 2 (依赖 Phase 1)
  ├─ 2.1 告警模块修复 ──┐
  ├─ 2.2 监控模块合并 ──┤
  ├─ 2.3 基础设施补测试 ─┤
  ├─ 2.4 FinOps修复 ────┤
  └─ 2.5 密码/MFA策略 ──┘
                          │
Phase 3 (依赖 Phase 2，可并行)
  ├─ 3.1 ITSM 新功能 ────┐
  ├─ 3.2 CMDB 新功能 ────┤
  ├─ 3.3 ChatOps AI 引擎 ─┤
  ├─ 3.4 身份认证增强 ────┤
  └─ 3.5 可观测性增强 ────┘
```

---

## 执行计划

### Phase 1A: 快速见效 (第 1 周)

**6 项任务无依赖，可 6 个 Agent 并行执行**

#### 1.1 注册 5 个未激活路由

| 维度 | 内容 |
|------|------|
| **域** | ChatOps AI |
| **工作量** | 0.5 天 |
| **优先级** | P0 |
| **依赖** | 无 |

**子任务**:

| # | 子任务 | 文件 | 工作量 |
|---|--------|------|--------|
| 1.1.1 | 将 `internal/ai-agent-run/` 加入 `ai_wiring.go` 初始化 | `cmd/server/ai_wiring.go` | 0.1 天 |
| 1.1.2 | 注册 `ai-agent-run` 路由到 `router.go` | `cmd/server/router.go` | 0.1 天 |
| 1.1.3 | 将 `internal/vector/` 加入 wiring + 注册路由 | `cmd/server/ai_wiring.go`, `router.go` | 0.1 天 |
| 1.1.4 | 将 `internal/prompt-security/` 加入 wiring + 注册路由 | `cmd/server/ai_wiring.go`, `router.go` | 0.1 天 |
| 1.1.5 | 将 `internal/semantic-search/` 加入 wiring + 注册路由 | `cmd/server/ai_wiring.go`, `router.go` | 0.05 天 |
| 1.1.6 | 将 `internal/rule-engine/` 加入 wiring + 注册路由 | `cmd/server/ai_wiring.go`, `router.go` | 0.05 天 |

**验收标准**:
- `go build ./cmd/server/...` 编译通过
- 5 个模块的所有路由可通过 `curl` 请求验证
- 5 个模块的 handler 变量在 `wiring.go` 中有声明

---

#### 1.2 废弃/冻结 identity/ 模块

| 维度 | 内容 |
|------|------|
| **域** | 身份认证 |
| **工作量** | 1 天 |
| **优先级** | P0 |
| **依赖** | 无 |

**子任务**:

| # | 子任务 | 文件 | 工作量 |
|---|--------|------|--------|
| 1.2.1 | 在 identity/ 目录根添加 `README.md` 标注为废弃 | `internal/identity/README.md` | 0.1 天 |
| 1.2.2 | 检查 `router.go` 中是否有 identity 子模块路由注册（应无） | `cmd/server/router.go` | 0.1 天 |
| 1.2.3 | 检查 `cicd_domain_wiring.go` 中是否有 identity 子模块 wiring（应无） | `cmd/server/cicd_domain_wiring.go` | 0.1 天 |
| 1.2.4 | 确认 `internal/identity/confirmation/` 是唯一非重叠模块，评估是否迁移 | `internal/identity/confirmation/` | 0.5 天 |
| 1.2.5 | 将 identity/confirmation 迁移为独立模块 `internal/confirmation/` | 新建目录 | 0.2 天 |

**验收标准**:
- identity/ 目录有废弃标注文档
- `internal/identity/confirmation/` 已迁移或标注待迁移
- 编译通过（无 identity 引用残留）

---

#### 1.3 为 logging 补充测试

| 维度 | 内容 |
|------|------|
| **域** | 监控可观测 |
| **工作量** | 1 天 |
| **优先级** | P0 |
| **依赖** | 无 |

**子任务**:

| # | 子任务 | 文件 | 工作量 |
|---|--------|------|--------|
| 1.3.1 | 为 logs 写入 handler 编写测试 (WriteLogs, BatchWrite) | `internal/logging/handler/handler_test.go` | 0.3 天 |
| 1.3.2 | 为 logs 查询 handler 编写测试 (QueryLogs, SearchLogs, AggregateLogs) | 同上 | 0.3 天 |
| 1.3.3 | 为 logs 清理 handler 编写测试 (CleanupOldLogs) | 同上 | 0.2 天 |
| 1.3.4 | 为 service 层编写测试 (WriteLog, QueryLog, SearchByKeyword, AggregateByTime) | `internal/logging/service/service_test.go` | 0.2 天 |

**验收标准**:
- 至少 10 个测试函数
- `go test ./internal/logging/...` 全部通过
- 覆盖正常路径 + 错误路径（空结果、无效参数、DB 错误）

---

#### 1.4 为 cost-allocation 添加权限控制

| 维度 | 内容 |
|------|------|
| **域** | FinOps |
| **工作量** | 0.5 天 |
| **优先级** | P0 |
| **依赖** | 无 |

**子任务**:

| # | 子任务 | 文件 | 工作量 |
|---|--------|------|--------|
| 1.4.1 | 为 cost-allocation 所有路由添加 `auth.RequirePermission("cost-allocation", "read"/"write")` | `internal/cost-allocation/handler/handler.go` | 0.3 天 |
| 1.4.2 | 验证路由注册正确性 | 编译检查 | 0.1 天 |
| 1.4.3 | 添加权限测试 | `internal/cost-allocation/handler/handler_test.go` | 0.1 天 |

**验收标准**:
- 所有路由有 `auth.RequirePermission` 守卫
- 无权限的请求返回 403
- 编译通过

---

#### 1.5 为 4 个核心模块补充测试

| 维度 | 内容 |
|------|------|
| **域** | 配置低代码 |
| **工作量** | 2-3 天 |
| **优先级** | P0 |
| **依赖** | 无 |

**子任务**:

| # | 子任务 | 文件 | 工作量 |
|---|--------|------|--------|
| 1.5.1 | extension-point 测试: 注册/发现/初始化/关闭/事件总线 | `internal/extension-point/*_test.go` | 1 天 |
| 1.5.2 | form 引擎测试: 验证/渲染/可见性/跨字段校验 | `internal/form/*_test.go` | 0.5 天 |
| 1.5.3 | import-export 测试: CSV/JSON 解析/Factory 模式/handler 注册 | `internal/import-export/*_test.go` | 0.5 天 |
| 1.5.4 | plugin-marketplace 测试: 发布/安装/卸载/评分 | `internal/plugin-marketplace/*_test.go` | 0.5 天 |

**验收标准**:
- 每个模块至少 10 个测试函数
- 所有测试通过
- 覆盖核心路径（创建/查询/更新/删除 + 错误路径）

---

#### 1.6 修复 finops-v2 桩实现

| 维度 | 内容 |
|------|------|
| **域** | FinOps |
| **工作量** | 1 天 |
| **优先级** | P0 |
| **依赖** | 无 |

**子任务**:

| # | 子任务 | 文件 | 工作量 |
|---|--------|------|--------|
| 1.6.1 | 实现 `CollectCost` 真实逻辑（从 DB 聚合成本数据） | `internal/finops-v2/handler/handler.go` | 0.5 天 |
| 1.6.2 | 实现 `GetBudgetStatus` 真实逻辑 | `internal/finops-v2/handler/handler.go` | 0.3 天 |
| 1.6.3 | 修复 `CollectCostResponse` 字段赋值错误 | `internal/finops-v2/models/models.go` | 0.1 天 |
| 1.6.4 | 补充测试 | `internal/finops-v2/handler/handler_test.go` | 0.1 天 |

**验收标准**:
- `CollectCost` 返回真实的成本聚合数据（非空值）
- `GetBudgetStatus` 返回真实预算状态（非 500 错误）
- 字段赋值正确
- 编译通过

---

### Phase 1B: 依赖 Phase 1A (第 2 周)

#### 1.7 实现 ABAC 策略评估引擎

| 维度 | 内容 |
|------|------|
| **域** | 身份认证 |
| **工作量** | 2-3 天 |
| **优先级** | P0 |
| **依赖** | 1.2 (废弃 identity/) |

**子任务**:

| # | 子任务 | 文件 | 工作量 |
|---|--------|------|--------|
| 1.7.1 | 设计评估引擎接口: `Evaluator.Evaluate(subject, resource, action, context) → Decision` | `internal/abac-policy/service/evaluator.go` | 0.5 天 |
| 1.7.2 | 实现条件表达式解析器 (支持 `eq`, `neq`, `in`, `contains`, `gt`, `lt` 操作符) | `internal/abac-policy/service/condition.go` | 0.5 天 |
| 1.7.3 | 实现策略组合算法 (deny-override / allow-override / first-match) | `internal/abac-policy/service/evaluator.go` | 0.5 天 |
| 1.7.4 | 暴露评估端点 `POST /api/v1/abac-policies/evaluate` | `internal/abac-policy/handler/handler.go` | 0.3 天 |
| 1.7.5 | 与 capability 模块集成 | `internal/capability/service/service.go` | 0.5 天 |
| 1.7.6 | 补充测试 | `internal/abac-policy/service/service_test.go` | 0.3 天 |

**验收标准**:
- `POST /api/v1/abac-policies/evaluate` 返回 `{allowed: true/false, effect: "allow"/"deny", matchedPolicies: []}`
- 支持 6 种条件操作符
- 3 种策略组合算法可选
- 测试覆盖 10+ 场景

---

### Phase 2: P1 修复 (第 3-4 周)

#### 2.1 告警模块修复

| 维度 | 内容 |
|------|------|
| **域** | 通知告警 |
| **工作量** | 5-7 天 |
| **优先级** | P1 |
| **依赖** | Phase 1 完成 |

**子任务**:

| # | 子任务 | 工作量 | 依赖 |
|---|--------|--------|------|
| 2.1.1 | 实现告警熔断器状态转换逻辑 (open/half-open/closed) | 1-2 天 | 无 |
| 2.1.2 | 统一去重算法为 SHA256 | 0.5 天 | 无 |
| 2.1.3 | 去重状态持久化到 Redis | 1 天 | 2.1.2 |
| 2.1.4 | 实现告警关联算法 (时间窗口 + 标签相似度 + 拓扑依赖) | 2-3 天 | 无 |
| 2.1.5 | 实现 SMS/Email 通知渠道 (SMTP 集成 + 短信网关) | 1-2 天 | 无 |

**验收标准**:
- 熔断器: 错误率 > 阈值 → open → half-open → 恢复 → closed
- 去重: SHA256 指纹，Redis 持久化，重启不丢失
- 关联: 时间窗口 + 标签相似度，置信度评分
- SMS/Email: 真实发送验证

#### 2.2 监控模块合并

| 维度 | 内容 |
|------|------|
| **域** | 监控可观测 |
| **工作量** | 1 天 |
| **优先级** | P1 |

**子任务**:
- 合并 `internal/observability/` 到 `internal/monitoring/`
- 合并 `internal/metrics/` 到 `internal/monitoring/`
- 清理 wiring 和路由注册

**验收标准**: 删除 2 个冗余模块，功能保留，编译通过

#### 2.3 基础设施补测试

| 维度 | 内容 |
|------|------|
| **域** | 基础设施安全 |
| **工作量** | 2-3 天 |

**子任务**:
- cluster: 补充 handler 测试 (8 方法)
- network: 补充 handler 测试 (28 方法)
- storage: 补充 handler 测试 (6 方法)
- self-healing: 补充 handler + service 测试

**验收标准**: 4 个模块各有 5+ 测试函数

#### 2.4 FinOps 修复

| 维度 | 内容 |
|------|------|
| **域** | FinOps |
| **工作量** | 5-8 天 |

**子任务**:
- 实现云成本数据采集 (AWS/GCP/Azure 成本 API 集成)
- 重构 capacity handler (77 方法 → 拆分)

#### 2.5 密码/MFA 策略

| 维度 | 内容 |
|------|------|
| **域** | 身份认证 |
| **工作量** | 2-3 天 |

**子任务**:
- 密码复杂度策略配置
- 密码历史检查
- 强制 MFA 策略 (可配置)
- 并发会话限制

---

### Phase 3: P2 优化 (第 5-8 周)

| # | 任务 | 域 | 工作量 | 依赖 |
|---|------|-----|--------|------|
| 3.1 | 新增 Release Management | ITSM | 3-5 天 | Phase 2 |
| 3.2 | 新增 Service Catalog | ITSM | 5-8 天 | Phase 2 |
| 3.3 | 新增 Drift Detection | CMDB | 3-5 天 | Phase 2 |
| 3.4 | 新增 AI CMDB 推荐 | CMDB | 5-10 天 | 3.3 |
| 3.5 | 实现向量嵌入引擎 | ChatOps AI | 3-5 天 | Phase 2 |
| 3.6 | 实现智能分析引擎 | ChatOps AI | 3-5 天 | 3.5 |
| 3.7 | 实现 FIDO2/WebAuthn | 身份认证 | 3-5 天 | 2.5 |
| 3.8 | 性能火焰图 | 监控可观测 | 3-5 天 | 2.2 |

---

## 时间线

```
Week 1          Week 2          Week 3-4        Week 5-8
├───────────┤   ├───────────┤   ├───────────┤   ├───────────────────┤
                                   
Phase 1A (并行)  Phase 1B        Phase 2          Phase 3
                                   
1.1 注册路由 ──┐                                    
1.2 废弃identity┤                                    
1.3 logging测试 ┤                                    
1.4 权限控制 ──┤   1.7 ABAC评估                     
1.5 核心测试 ──┤   引擎(2-3d)    2.1 告警修复(5-7d)   3.1 Release(3-5d)  
1.6 finops修复 ┤                   2.2 监控合并(1d)    3.2 ServiceCat(5-8d)
                                   2.3 基础测试(2-3d)  3.3 Drift(3-5d)
                                   2.4 FinOps(5-8d)   3.4 AI CMDB(5-10d)
                                   2.5 密码MFA(2-3d)  3.5 向量引擎(3-5d)
                                                      3.6 智能分析(3-5d)
                                                      3.7 FIDO2(3-5d)
                                                      3.8 火焰图(3-5d)
```

---

## Agent 执行分配

| Agent | Phase 1A | Phase 1B | Phase 2 | Phase 3 |
|-------|----------|----------|---------|---------|
| **Agent A** (ChatOps AI) | 1.1 注册路由 | — | — | 3.5 向量引擎, 3.6 智能分析 |
| **Agent B** (身份认证) | 1.2 废弃identity | 1.7 ABAC引擎 | 2.5 密码MFA | 3.7 FIDO2 |
| **Agent C** (监控可观测) | 1.3 logging测试 | — | 2.2 监控合并 | 3.8 火焰图 |
| **Agent D** (FinOps) | 1.4 权限控制, 1.6 finops修复 | — | 2.4 FinOps | — |
| **Agent E** (配置低代码) | 1.5 核心测试 | — | — | — |
| **Agent F** (通知告警) | — | — | 2.1 告警修复 | — |
| **Agent G** (基础设施) | — | — | 2.3 基础测试 | — |
| **Agent H** (ITSM/CMDB) | — | — | — | 3.1 Release, 3.2 ServiceCat, 3.3 Drift, 3.4 AI CMDB |

---

## 风险标注

| 风险 | 任务 | 影响 | 缓解措施 |
|------|------|------|---------|
| 🔴 ABAC 评估引擎与 capability 模块集成复杂 | 1.7 | 延期 1-2 天 | 先做独立评估引擎，集成放后期 |
| 🔴 云成本采集需云厂商 API 密钥 | 2.4 | 阻塞 | 先做 Mock 实现，密钥配置做外部化 |
| 🟡 告警关联算法需性能验证 | 2.1.4 | 性能不达标 | 先用时间窗口 + 标签简单匹配 |
| 🟡 SMS 渠道需第三方网关 | 2.1.5 | 阻塞 | 先做 SMTP 邮件，SMS 做接口预留 |
| 🟢 5 个未激活路由可能已有前端依赖 | 1.1 | 路由冲突 | 检查前端 API 路径后注册 |

---

## 完成标准 (Phase 1-3 全部完成)

| 指标 | 当前值 | 目标值 | 提升 |
|------|--------|--------|------|
| 架构健康度 | 7.5/10 | **8.5/10** | +1.0 |
| 测试覆盖 | 12 模块零测试 | **0 模块零测试** | 100% |
| 路由注册完整度 | 5 未注册 | **0 未注册** | 100% |
| 权限控制完整度 | 1 模块缺失 | **0 模块缺失** | 100% |
| 桩实现 | 8 个 | **0 个** | 100% |
| 内存存储 | 3 个 | **0 个** | 100% |
| 功能重叠 | 7 处 | **0 处** | 100% |
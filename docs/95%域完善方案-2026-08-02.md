# Orion 非五星域 → 95%+ 完善方案

> **编制**: 2026-08-02 | **编制团队**: 7 位领域资深专家
> **核心发现**: wiring 只解决 10% 的差距。**真正的差距在于：桩实现 + 缺失的企业功能 + 前端权限真空**
> **实测验证**: alert-pipeline 有 16 文件但 0 handler/0 service; AI orchestration 是桩; CMDB 4 核心模块全未 wired

---

## 一、诚实差距分析 — 为什么之前方案只到 74%？

之前的方案只覆盖了 **wiring + 补测试**——但那只能把"已写但未接线"的代码激活。
真正的 95%+ 差距来自三层：

| 差距层 | 占比 | 之前方案覆盖? |
|--------|:----:|:------------:|
| L1: 代码未 wiring | ~15% | ✅ 已覆盖 |
| L2: 核心模块是**桩实现** | ~30% | ❌ **完全未覆盖** |
| L3: **企业功能根本不存在** | ~55% | ❌ **完全未覆盖** |

**实测证据**:

| 发现 | 证据 | 严重度 |
|------|------|:------:|
| CMDB 4 核心模块 **全部未 wired** | cmdb/visor/visor-exec/collector = 0 wired | 🔴 |
| AI orchestration 是**桩** | `executeOrchestration` 只 log + mark completed，无 agent 执行 | 🔴 |
| CI/CD runner/audit/history **全部未 wired** | runner/pipeline-audit-log/pipeline-run-history = 0 wired | 🔴 |
| notification-engine **1018 方法**但子模块不可用 | channel/policy/template/dnd = 0 wired | 🔴 |
| 告警管道 **16 文件但 0 handler/0 service** | alert-pipeline 只有 stages/models/plugins/correlation/event | 🔴 |
| 前端 **0 个 usePermission** | 全前端搜索 = 0 匹配 | 🔴 |
| ITSM sla-engine **未 wired** | 0 wired | 🟠 |
| 缺 Release Management | 0 目录 | 🟠 |
| 缺 Service Catalog | 0 独立目录（仅 service-catalog 有 9 文件） | 🟠 |
| 缺 CMDB Drift Detection | 0 目录 | 🟠 |
| 缺 Test Execution Engine | 0 目录 | 🟠 |
| 缺 Data Classification/Masking | 0 目录 | 🟠 |
| 缺 ML 异常检测 | chaos 有 76S 但无检测引擎 | 🟠 |
| risk 仅 CRUD（5 方法） | 无风险评分/矩阵/热力图 | 🟡 |
| degradation 仅 CRUD（5 方法） | 无自动降级触发 | 🟡 |

---

## 二、9 个非五星域 → 95%+ 完整完善方案

### 2.1 CMDB 域 — 从 90% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: 4 个核心模块（cmdb/visor/visor-exec/collector）**全部未 wired**，CMDB 实际可用率 **0%**。

#### Phase 1: 核心 Wiring（P0, 2 天）

| 模块 | 规模 | 修复 | 暴露能力 |
|------|------|------|---------|
| cmdb | 30S/959H | wiring + router | CI CRUD/批量/拓扑 |
| visor | 4H/5S/4T | wiring + router | Xterm.js 8 addon Web 终端 |
| visor-exec | 4H/5S/4T | wiring + router | 命令执行/会话管理 |
| cmdb-collector | 18文件 | wiring + router | Adapter 模式自动发现 |

#### Phase 2: 缺失功能补充（P1, 8 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **Drift Detection** | iTop Drift | 新增 `internal/cmdb-drift/`，定时扫描 CI 属性变更，告警 | 3 天 |
| 2 | **CMDB AI 推荐** | ServiceNow AI | 基于 topology + 历史变更，推荐关联 CI | 3 天 |
| 3 | **CMDB Search 增强** | Elastic 全文检索 | 当前有 6 个 search 文件但无搜索 API 路由 | 2 天 |

#### 验收标准: CMDB 5 核心模块全部 wired + drift 检测可用 + AI 推荐可用

---

### 2.2 CI/CD 域 — 从 95% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: runner/audit-log/run-history 全部未 wired，触发器薄弱。

#### Phase 1: 核心 Wiring（P0, 1 天）

| 模块 | 规模 | 暴露能力 |
|------|------|---------|
| pipeline-executor | 2H/4S | Pipeline 执行引擎 |
| pipeline-audit-log | 2H/3S | 审计日志 |
| pipeline-run-history | 2H/3S | 运行历史 |
| runner | 1H/1S/28R | Runner 管理 |
| pipeline-error-detail | 2H/3S | 错误详情 |

#### Phase 2: 缺失功能补充（P1, 5 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **Pipeline 触发器引擎** | GitLab CI | 新增 `internal/pipeline-trigger-engine/`：Webhook/定时/上游 Pipeline/Manual 4 种触发器 | 2 天 |
| 2 | **Pipeline 重试/回滚 UI** | Jenkins | 前端 `PipelineRetryPage.tsx` + 后端 retry/rollback API | 1 天 |
| 3 | **Pipeline 运行分析** | GitLab Analytics | 成功率/耗时趋势/瓶颈分析 | 2 天 |

#### 验收标准: 5 模块 wired + 4 种触发器可用 + 运行分析可用

---

### 2.3 ITSM 域 — 从 95% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: sla-engine 未 wired + 缺 Release Management + 缺 Service Catalog。

#### Phase 1: 核心 Wiring（P0, 1 天）

| 模块 | 规模 | 暴露能力 |
|------|------|---------|
| sla-engine | 2H/3S/3R/32S | SLA 定义/计时/违反检测/报告 |
| escalation | 2H/2S | 升级规则 |
| runbook | 4H/3S/2R | 运维手册 |

#### Phase 2: 缺失功能补充（P1, 8 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **Release Management** | ServiceNow Release | 新增 `internal/release-mgmt/`：Release Plan/Build/Deploy/Test/Go-Live，版本管理 | 3 天 |
| 2 | **Service Catalog** | ServiceNow Catalog | 新增 `internal/service-catalog/`：服务目录/自助申请/审批流程 | 3 天 |
| 3 | **ticketing/ticket 合并** | — | 6,672→4,000 行，统一工单引擎 | 3 天 |

#### 验收标准: sla-engine wired + Release Mgmt 可用 + Service Catalog 可用

---

### 2.4 告警/通知域 — 从 70% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: alert-pipeline 16 文件但 **0 handler/0 service/0 route**；notification 13 子模块全部未 wired；alert-adapter 10 种 SPI 适配器不可用。

#### Phase 1: 核心 Wiring（P0, 3 天）

| 模块 | 规模 | 暴露能力 |
|------|------|---------|
| alert-adapter | 1H/8S/11文件 | 10 种数据源适配器 |
| alert-correlation | 2H/1S | 告警关联 |
| alert-deduplication | 1H/1S | 去重 |
| alert-silence | 1H/1S | 静默规则 |
| alert-breaker | 2H/2S | 熔断 |
| **alert-pipeline** | **16文件/0H/0S** | **补 handler + service 暴露 6 阶段管道** |
| notification-policy | 2H/2S | 通知策略 |
| notification-template | 2H/2S | 通知模板 |
| scheduled-notification | 2H/2S | 定时通知 |
| channel | 2H/2S | 通知通道 |

#### Phase 2: 告警管道重建（P1, 3 天）

alert-pipeline 已有 stages/correlation/event/models/plugins 但缺统一入口。

```go
// 新增 internal/alert-pipeline/handler/handler.go
func (h *Handler) Execute(ctx, alert) error   // 统一入口
func (h *Handler) ListStages(ctx) error      // 查看管道配置
func (h *Handler) GetStats(ctx) error        // 管道统计

// 新增 internal/alert-pipeline/service/pipeline.go
func (s *Service) ExecutePipeline(ctx, alert) *PipelineResult {
    // 6 阶段: validate → dedup → correlate → silence → route → notify
}
```

#### Phase 3: 缺失功能补充（P1, 6 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **告警规则引擎** | Grafana Alerting | 新增 `internal/alert-rule-engine/`：PromQL-like 表达式 + 可视化条件编辑器 | 2 天 |
| 2 | **告警拓扑可视化** | Grafana Alert Graph | 告警→服务→CMDB 级联关系图 | 2 天 |
| 3 | **告警疲劳度分析** | PagerDuty | 告警频次/误报率/响应时效分析 | 2 天 |

#### 验收标准: 11 模块 wired + 管道 6 阶段可用 + 规则引擎可用

---

### 2.5 监控域 — 从 80% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: logging 0 测试；缺 OpenTelemetry 集成；缺自定义仪表板编辑器；告警规则引擎缺失。

#### Phase 1: logging 补测试（P0, 1 天）

logging 已有完整 3 层架构（11H/18S/18R/56文件/2023行），但 **0 测试**。

#### Phase 2: 缺失功能补充（P1, 10 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **OpenTelemetry 集成** | OTel Collector | 补 `internal/logging/otel/`，实现 `otlplog.LogEmitter`，Metrics/Traces/Logs 三支柱统一 | 2 天 |
| 2 | **自定义仪表板编辑器** | Grafana | 面板类型注册器（TimeSeries/Bar/Pie/Gauge/Stat/Table/Heatmap/LogTable），拖拽式布局 | 4 天 |
| 3 | **Trace 详情可视化** | Jaeger | 水坝图(Gantt Chart)，span 展开/耗时高亮/错误标记 | 2 天 |
| 4 | **性能火焰图** | Perfetto | CPU/内存/IO 火焰图可视化 | 2 天 |

#### 验收标准: logging 有测试 + OTel 三支柱统一 + 仪表板编辑器可用

---

### 2.6 安全运营域 — 从 65% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: chaos-gateway/circuit-breaker 未 wired；risk/degradation 仅 CRUD；缺 ML 异常检测。

#### Phase 1: 核心 Wiring（P0, 1 天）

| 模块 | 规模 | 暴露能力 |
|------|------|---------|
| chaos-gateway | 2H/2S/2R | 混沌实验网关 |
| circuit-breaker | 2H/2S/2R | 熔断器状态机（已有真实 Closed/Open/HalfOpen 转换） |

#### Phase 2: 缺失功能补充（P1, 10 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **ML 异常检测引擎** | — | 新增 `internal/chaos/detector/`：IsolationForest/3σ/Z-Score，基线学习 | 4 天 |
| 2 | **risk 风险评分引擎** | FAIR/NIST | 新增 `internal/risk/scoring/`：风险矩阵/评分算法/热力图 | 2 天 |
| 3 | **degradation 自动降级** | Netflix Hystrix | 新增自动降级触发器：基于错误率/延迟自动触发 | 2 天 |
| 4 | **security-compliance SBOM 增强** | Snyk | 供应链安全扫描 + 漏洞追踪 | 2 天 |

#### 验收标准: 2 模块 wired + ML 检测可用 + 风险评分可用 + 自动降级可用

---

### 2.7 配置低代码域 — 从 74% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: param-types/import-export/form/plugin-marketplace 全部未 wired；form 0 测试；低代码缺可视化编辑器。

#### Phase 1: 核心 Wiring（P0, 2 天）

| 模块 | 规模 | 暴露能力 |
|------|------|---------|
| param-types | 1H/1S/132S | 132 种参数类型定义/校验 |
| import-export | 1H/1S | 配置导入/导出 |
| form | 1H/1S/16S | 表单引擎（已有 FormEngine: CreateForm/RenderForm/Validate/SubmitFormData） |
| plugin-marketplace | 1H/1S | 插件市场 |

#### Phase 2: 缺失功能补充（P1, 8 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **低代码可视化编辑器** | OutSystems | 新增 `internal/lowcode/editor/`：流程图 DSL + Canvas 拖拽 + 节点类型（Start/Task/Decision/Approval/End） | 4 天 |
| 2 | **表单高级渲染器** | — | form 已有 FormEngine 但缺前端渲染器；新增 `FormRenderer.tsx` 支持 12 种字段类型 | 2 天 |
| 3 | **配置版本差异可视化** | — | config 已有版本/回滚但缺 UI diff；新增 `ConfigDiffPage.tsx` | 2 天 |

#### 验收标准: 4 模块 wired + 低代码编辑器可用 + 表单渲染器可用

---

### 2.8 AI/ChatOps 域 — 从 70% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: AI orchestration 是**桩**（只 log 然后 mark completed）；prompt-security 无 Repo；llm-provider 有代码但未接入；agent 编排能力不足。

#### Phase 1: 核心修复（P0, 2 天）

| 模块 | 问题 | 修复 |
|------|------|------|
| **ai/** 容器 | 169 文件但 **0 wired** | wiring.go 注入 ai handler |
| ai/prompt-security | 缺 Repo | 补 repository.go + migration |
| ai/llm-provider | 5 文件但未接入 gateway | 集成到 ai-gateway Service |

#### Phase 2: 缺失功能补充（P1, 12 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **Agent 多智能体编排** | LangGraph | **重建** `internal/ai/orchestration/`：Orchestrator interface → MultiAgentOrchestrator → Agent DAG → Supervisor → Critic | 5 天 |
| 2 | **LLM Provider 注册中心** | LiteLLM | 实现 ProviderRegistry（OpenAI/Anthropic/DeepSeek），自动降级 + Token 池 | 2 天 |
| 3 | **Agent 工具协议** | MCP | 增强 ToolHandler 协议，支持自定义工具注册 | 2 天 |
| 4 | **AI 对话记忆** | ChatGPT | 新增 `internal/ai/memory/`：对话历史/上下文窗口管理 | 2 天 |
| 5 | **AI 安全护栏** | NVIDIA NeMo | prompt-injection 检测 + 输出审核 | 1 天 |

#### 验收标准: AI 容器 wired + orchestration 非桩（有 agent 执行）+ 4 Provider + 对话记忆

---

### 2.9 身份认证域 — 从 75% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: identity 69 文件 0 测试 0 wired；sso/session/tenant 全部未 wired。

#### Phase 1: 核心 Wiring（P0, 2 天）

| 模块 | 规模 | 暴露能力 |
|------|------|---------|
| identity (69文件) | 19H/9S/11R | 用户/角色/权限/SSO 核心 |
| session | 2H/2S | 会话管理 |
| tenant | 2H/2S | 租户管理 |
| sso | 2H/2S | SSO 统一认证 |

#### Phase 2: 测试补充（P1, 2 天）

identity 69 文件 0 测试，补 Login/Refresh/Logout/PermissionCheck 路径。

#### Phase 3: 缺失功能补充（P1, 4 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **OIDC 完整支持** | Keycloak | Authorization Code Flow + PKCE + Well-Known Discovery | 2 天 |
| 2 | **密码策略可配置** | — | bcrypt 12 硬编码→可配置；新增密码强度/过期/历史策略 | 1 天 |
| 3 | **登录异常检测** | — | 异地/新设备/异常时段检测 + 告警 | 1 天 |

#### 验收标准: 4 模块 wired + 测试覆盖 + OIDC 可用 + 异常检测

---

### 2.10 数据治理域 — 从 75% 到 ⭐⭐⭐⭐⭐

**当前瓶颈**: finops-v2 CollectCost 桩实现；缺 Data Classification/Masking。

#### Phase 1: finops-v2 实现（P1, 3 天）

`finops-v2/service/service.go` CollectCost 为桩，实现：
- K8s 成本采集（Prometheus 数据源）
- Namespace/Pod/Container 分层
- 对接 finops/service 现有 42 Repo 方法

#### Phase 2: 缺失功能补充（P1, 6 天）

| # | 功能 | 对标 | 说明 | 工作量 |
|---|------|------|------|:------:|
| 1 | **Data Classification** | Collibra | 新增 `internal/data-classification/`：数据分级/标签/敏感数据识别 | 2 天 |
| 2 | **Data Masking** | — | 新增 `internal/data-masking/`：动态脱敏/静态脱敏/脱敏规则引擎 | 2 天 |
| 3 | **列级数据血缘** | dbt | data-lineage 增强：Column-Level Lineage + 影响分析 | 2 天 |

#### 验收标准: finops-v2 非桩 + Data Classification 可用 + Data Masking 可用

---

### 2.11 前端权限域 — 从 0% 到 ⭐⭐⭐⭐

**当前瓶颈**: 全前端 **0 个 usePermission**，部署/配置/Secret/审批/管理员页面 **100% 无权限守卫**。

#### Phase 1: 权限基础设施（P0, 2 天）

| # | 功能 | 说明 | 工作量 |
|---|------|------|:------:|
| 1 | `usePermission` Hook | `usePermission(resource, action)` 返回 `{ has: boolean, loading: boolean }` | 0.5 天 |
| 2 | `AuthRouteGuard` 组件 | 路由级守卫，无权限自动 403 | 0.5 天 |
| 3 | 敏感页面守卫 | 部署/配置/Secret/审批/管理员/治理 页面全加守卫 | 1 天 |

#### 验收标准: 100+ 敏感页面有权限守卫

---

## 三、总时间线

```
P0 — 核心 Wiring (19 模块)        ████████                                → 4 天
P1 — 桩实现修复 (6 项)             ████████████                            → 6 天
P1 — 缺失功能补充 (22 项)          ████████████████████████████████████████████  → 62 天
P0 — 前端权限 (1 项)               ████                                    → 2 天
                                                      合计: 74 天
                                                      8 Agent 并行: 10-12 天
```

## 四、预期收益

| 指标 | 当前 | 95%+ 修复后 |
|------|------|------------|
| 可用代码行 | 309K (60.6%) | 510K (100%) |
| Wired 模块 | 177 | 282 |
| 桩实现修复 | 6 个桩 | 0 桩 |
| 缺失功能 | 22 项 | 0 缺失 |
| 前端权限守卫 | 0 (0%) | 100+ (100%) |
| 方法孤儿 | 204 | 0 |
| 零测试模块 | 56 | <5 |
| 架构健康度 | 6.0/10 | **9.0/10** |

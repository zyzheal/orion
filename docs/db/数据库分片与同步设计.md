# Orion 平台数据库分片与 ClickHouse 同步设计

> 版本：v1.0  
> 日期：2026-04-10  
> 状态：设计方案  
> 适用范围：42 张表中的高增长日志类表

---

## 一、背景与目标

### 1.1 问题陈述

架构师和后端评审发现 Orion CMDB 42 张表中存在以下缺陷：

| 问题 | 影响 | 紧迫性 |
|------|------|--------|
| 5 张高增长表无分库分表策略 | 6 个月后单表可能突破 5000 万行 | 高 |
| MySQL → ClickHouse 同步方案缺失 | 无法支持实时分析查询 | 高 |
| 数据归档策略未定义 | 热温冷数据混存，查询性能下降 | 中 |

### 1.2 设计目标

1. **分片策略**：为高增长表设计合理的分库分表方案
2. **同步方案**：建立 MySQL 到 ClickHouse 的实时数据同步
3. **一致性保障**：确保同步过程中的数据一致性和可恢复性
4. **生命周期管理**：实现热/温/冷数据自动归档

---

## 二、分片表分类

### 2.1 表分类总览

```mermaid
graph TB
    subgraph 需要分片的表
        A1[audit_log<br/>审计日志]
        A2[event_log<br/>事件日志]
        A3[cicd_pipeline_run<br/>流水线运行]
        A4[ai_inference_log<br/>AI 推理日志]
        A5[k8s_sync_log<br/>K8s 同步日志]
    end
    
    subgraph 不需要分片的表
        B1[主机资产表 8 张<br/>低频配置数据]
        B2[K8s 资源表 9 张<br/>不含 sync_log]
        B3[CI/CD 配置表 5 张<br/>不含 run/artifact]
        B4[GitOps 表 4 张<br/>配置型数据]
        B5[AI 资源表 3 张<br/>不含 inference_log]
        B6[公共配置表 7 张<br/>字典/模板等]
    end
    
    A1 --> C[分片策略设计]
    A2 --> C
    A3 --> C
    A4 --> C
    A5 --> C
    
    B1 --> D[保持单表<br/>建立适当索引]
    B2 --> D
    B3 --> D
    B4 --> D
    B5 --> D
    B6 --> D
```

### 2.2 分片表详细分析

| 表名 | 当前量级 | 6 个月预估 | 分片原因 | 分片优先级 |
|------|----------|-----------|----------|-----------|
| audit_log | 100 万/月 | 600 万/月 | 全量审计写入，查询按时间 | P0 |
| event_log | 50 万/月 | 300 万/月 | 事件溯源，时间序列特征 | P0 |
| cicd_pipeline_run | 10 万/月 | 60 万/月 | CI/CD 高频运行 | P1 |
| ai_inference_log | 20 万/月 | 120 万/月 | AI 调用快速增长 | P1 |
| k8s_sync_log | 30 万/月 | 180 万/月 | 多集群同步，按集群分区 | P1 |

---

## 三、分片键设计（Shard Key）

### 3.1 分片键选择原则

```mermaid
flowchart TD
    A[选择分片键] --> B{数据特征分析}
    B -->|时间序列 | C[时间分片]
    B -->|多租户 | D[租户分片]
    B -->|多集群 | E[集群分片]
    B -->|混合特征 | F[复合分片]
    
    C --> G[评估查询模式]
    D --> G
    E --> G
    F --> G
    
    G --> H{查询是否包含分片键？}
    H -->|是 | I[直接路由到分片]
    H -->|否 | J[广播查询 + 结果聚合]
    
    I --> K[分片键确定]
    J --> K
```

### 3.2 各表分片键设计

| 表名 | 主分片键 | 次分片键 | 选择理由 |
|------|----------|----------|----------|
| audit_log | tenant_id + 时间 (月) | - | 查询多按租户 + 时间范围 |
| event_log | tenant_id + 时间 (月) | event_type | 租户隔离，事件类型过滤 |
| cicd_pipeline_run | tenant_id + 时间 (月) | pipeline_id | 租户隔离，支持流水线维度查询 |
| ai_inference_log | tenant_id + 时间 (月) | model_id | 租户隔离，支持模型维度分析 |
| k8s_sync_log | cluster_id + 时间 (月) | - | 按集群自然分区 |

### 3.3 分片键命名规范

```
分片表命名：{表名}_{分片键值}_{时间范围}

示例：
- audit_log_001_202604  (租户 1, 2026 年 4 月)
- audit_log_002_202604  (租户 2, 2026 年 4 月)
- k8s_sync_log_cluster1_202604
```

---

## 四、分片算法设计

### 4.1 分片算法选择

```mermaid
graph LR
    subgraph 分片算法
        A[Hash 分片] --> B[均匀分布<br/>不支持范围查询]
        C[Range 分片] --> D[支持范围查询<br/>可能数据倾斜]
        E[时间分区] --> F[自然归档<br/>时间范围查询高效]
        G[复合分片] --> H[多维度路由<br/>实现复杂]
    end
    
    subgraph Orion 选择
        A --> I[不采用<br/>审计日志需要时间范围查询]
        C --> J[不采用<br/>租户数据量差异大]
        E --> K[✓ 主选方案<br/>日志类数据天然适合]
        G --> L[✓ 辅助方案<br/>租户 + 时间复合]
    end
```

### 4.2 时间分区策略

| 表名 | 分区粒度 | 分区数量预估 | 单分区容量 |
|------|----------|-------------|-----------|
| audit_log | 按月 | 12 个月滚动 | 500-1000 万行 |
| event_log | 按月 | 12 个月滚动 | 200-500 万行 |
| cicd_pipeline_run | 按月 | 12 个月滚动 | 50-100 万行 |
| ai_inference_log | 按月 | 12 个月滚动 | 100-200 万行 |
| k8s_sync_log | 按月×集群 | 12 月×10 集群 | 10-50 万行/分区 |

### 4.3 分区管理流程

```mermaid
sequenceDiagram
    participant M as MySQL 主库
    participant S as 分片路由层
    participant P as 分区管理器
    participant D as 归档服务
    
    Note over M,D: 日常写入流程
    M->>S: 写入请求
    S->>P: 查询当前分区
    P-->>S: 返回分区表名
    S->>M: 路由到具体分区
    
    Note over M,D: 月度分区创建
    rect rgb(200, 240, 200)
        P->>P: 每月 25 日检查
        P->>M: CREATE PARTITION 下月分区
        P->>P: 更新分区路由表
    end
    
    Note over M,D: 季度数据归档
    rect rgb(240, 220, 200)
        P->>P: 每季度检查 3 月前数据
        P->>D: 触发归档任务
        D->>M: 导出数据到冷存储
        D->>M: DROP 已归档分区
    end
```

---

## 五、跨分片查询方案

### 5.1 查询路由架构

```mermaid
graph TB
    subgraph 应用层
        App[应用程序]
        SDK[分片查询 SDK]
    end
    
    subgraph 路由层
        Router[查询路由器]
        Meta[分片元数据]
    end
    
    subgraph 数据层
        Shard1[分片 1<br/>tenant_1_202604]
        Shard2[分片 2<br/>tenant_1_202605]
        Shard3[分片 3<br/>tenant_2_202604]
    end
    
    App --> SDK
    SDK --> Router
    Router --> Meta
    
    Meta -.->|路由规则 | Router
    
    Router -->|精确路由 | Shard1
    Router -->|精确路由 | Shard2
    Router -->|广播查询 | Shard1
    Router -->|广播查询 | Shard2
    Router -->|广播查询 | Shard3
    
    style Meta fill:#f9f,stroke:#333
```

### 5.2 查询场景与路由策略

| 查询类型 | 示例 | 路由方式 | 性能特征 |
|----------|------|----------|----------|
| 精确分片查询 | `WHERE tenant_id=1 AND time='2026-04'` | 直接路由 | 最优 |
| 租户范围查询 | `WHERE tenant_id=1 AND time BETWEEN ...` | 多分片扫描 | 良好 |
| 跨租户聚合 | `SELECT COUNT(*) GROUP BY tenant_id` | 广播 + 聚合 | 较慢 |
| 全表扫描 | `SELECT * LIMIT 100` | 最新分片 | 可接受 |

### 5.3 跨分片查询流程

```mermaid
flowchart TD
    Q[查询请求] --> A{是否包含分片键？}
    
    A -->|包含完整分片键 | B[单分片查询]
    A -->|包含部分分片键 | C[确定分片范围]
    A -->|不包含分片键 | D[广播查询]
    
    B --> E[路由到目标分片]
    C --> F[并行查询多个分片]
    D --> G[查询所有分片]
    
    E --> H[返回结果]
    F --> I[结果合并/排序]
    G --> I
    
    I --> H
    
    style B fill:#9f9
    style E fill:#9f9
    style H fill:#9f9
```

---

## 六、MySQL → ClickHouse CDC 同步方案

### 6.1 整体架构

```mermaid
graph TB
    subgraph 数据源层
        MySQL[MySQL 主库<br/>分片表]
        Binlog[MySQL Binlog]
    end
    
    subgraph CDC 采集层
        Flink[Flink CDC<br/>/ Maxwell]
        Kafka[Kafka 消息队列]
    end
    
    subgraph 同步处理层
        ETL[数据转换服务]
        Checkpoint[检查点管理]
    end
    
    subgraph 数据目标层
        CH[ClickHouse 集群<br/>分布式表]
        CHReplica[ClickHouse 副本]
    end
    
    subgraph 监控层
        Monitor[数据一致性监控]
        Alert[延迟告警]
    end
    
    MySQL -->|Binlog | Flink
    Binlog -.-> Flink
    Flink -->|JSON 消息 | Kafka
    Kafka --> ETL
    ETL --> Checkpoint
    Checkpoint --> CH
    
    Monitor --> Flink
    Monitor --> CH
    Monitor --> Alert
    
    style MySQL fill:#e3fcef
    style CH fill:#deebff
    style Kafka fill:#fff4e6
```

### 6.2 同步表映射关系

| MySQL 表 | ClickHouse 表 | 同步模式 | 延迟要求 |
|----------|--------------|----------|----------|
| audit_log_* | audit_log_all | 实时 | < 5 秒 |
| event_log_* | event_log_all | 实时 | < 5 秒 |
| cicd_pipeline_run_* | cicd_pipeline_run_all | 实时 | < 10 秒 |
| ai_inference_log_* | ai_inference_log_all | 实时 | < 5 秒 |
| k8s_sync_log_* | k8s_sync_log_all | 准实时 | < 30 秒 |

### 6.3 CDC 同步详细流程

```mermaid
sequenceDiagram
    participant M as MySQL
    participant C as Flink CDC
    participant K as Kafka
    participant E as ETL 服务
    participant H as ClickHouse
    participant V as 一致性校验
    
    Note over M,V: 初始全量同步
    M->>C: 读取当前 Binlog 位点
    C->>M: 全量数据快照
    M-->>C: 历史数据
    C->>K: 写入快照消息
    
    Note over M,V: 增量同步循环
    loop 持续同步
        M->>M: 数据变更
        M->>C: Binlog 事件
        C->>K: 转换后消息
        K->>E: 消费消息
        E->>E: 数据转换/清洗
        E->>H: Batch 写入
        H-->>E: 写入确认
    end
    
    Note over M,V: 一致性校验
    V->>M: 采样 MySQL 数据
    V->>H: 采样 ClickHouse 数据
    V->>V: 比对校验
    V-->>E: 差异告警
```

### 6.4 ClickHouse 表结构设计要点

```mermaid
graph TB
    subgraph ClickHouse 表设计
        A[分布式表<br/>_all 后缀]
        B[本地表<br/>_local 后缀]
        C[ReplicatedMergeTree]
    end
    
    subgraph 引擎选择
        D[MergeTree 家族]
        E[ReplacingMergeTree<br/>去重]
        F[SummingMergeTree<br/>预聚合]
        G[AggregatingMergeTree<br/>预计算]
    end
    
    A -->|写入 | B
    B --> C
    D --> E
    D --> F
    D --> G
    
    style A fill:#deebff
    style C fill:#9f9
```

---

## 七、数据一致性保障

### 7.1 一致性模型

| 场景 | 一致性级别 | 实现方式 | 容忍延迟 |
|------|-----------|----------|----------|
| 实时查询 | 最终一致性 | CDC 异步同步 | 5-10 秒 |
| 报表分析 | 最终一致性 | 批量聚合 | 分钟级 |
| 对账审计 | 强一致性 | 定时校验 + 修复 | T+1 |

### 7.2 延迟容忍策略

```mermaid
flowchart LR
    A[数据写入 MySQL] --> B{业务场景}
    
    B -->|实时性要求高 | C[双写模式<br/>MySQL + CH]
    B -->|可接受延迟 | D[CDC 异步同步]
    B -->|仅分析查询 | E[仅查询 CH<br/>容忍延迟]
    
    C --> F[应用层读路由<br/>优先 CH, 缺失回退 MySQL]
    D --> G[读取 CH<br/>提示数据延迟]
    E --> G
    
    F --> H[最终一致性保障]
    G --> H
```

### 7.3 冲突解决策略

| 冲突类型 | 检测方式 | 解决策略 |
|----------|----------|----------|
| 重复写入 | 主键冲突 | 以 MySQL 为准，CH 执行 REPLACE |
| 乱序更新 | 时间戳比对 | 保留最新版本 |
| 删除同步 | 软删除标记 | CDC 捕获 deleted=1 同步删除 |
| 数据差异 | 定时 checksum | 自动重放 Binlog 修复 |

### 7.4 故障恢复流程

```mermaid
sequenceDiagram
    participant M as 监控服务
    participant C as Flink CDC
    participant H as ClickHouse
    participant R as 恢复服务
    
    M->>M: 检测到同步延迟>阈值
    M->>C: 暂停同步
    M->>R: 触发告警
    
    R->>H: 查询当前位点
    R->>C: 获取 CDC 位点
    R->>R: 计算差异
    
    alt 差异较小
        R->>C: 从检查点重放
        C->>H: 补全数据
    else 差异较大
        R->>M: 触发全量重同步
        R->>H: TRUNCATE 目标表
        R->>C: 重新全量同步
    end
    
    R->>M: 恢复完成
    M->>C: 恢复同步
```

---

## 八、数据归档策略

### 8.1 热/温/冷数据定义

```mermaid
graph TB
    subgraph 数据分层
        H[热数据<br/>最近 3 个月]
        W[温数据<br/>3-12 个月]
        C[冷数据<br/>12 个月以上]
    end
    
    subgraph 存储位置
        H --> MySQL[MySQL 分片表<br/>SSD]
        W --> CH[ClickHouse<br/>HDD/SSD 混合]
        C --> Cold[对象存储/OSS<br/>归档存储]
    end
    
    subgraph 查询方式
        MySQL --> Q1[实时查询<br/>< 100ms]
        CH --> Q2[分析查询<br/>< 1s]
        Cold --> Q3[离线查询<br/>分钟级]
    end
    
    style H fill:#d4edda
    style W fill:#fff3cd
    style C fill:#f8d7da
```

### 8.2 数据生命周期

| 表名 | 热数据保留 | 温数据保留 | 冷数据保留 | 自动删除 |
|------|-----------|-----------|-----------|----------|
| audit_log | 3 个月 | 12 个月 | 永久 | 可选 |
| event_log | 3 个月 | 12 个月 | 永久 | 可选 |
| cicd_pipeline_run | 6 个月 | 24 个月 | 永久 | 可选 |
| ai_inference_log | 3 个月 | 6 个月 | 12 个月 | 是 |
| k8s_sync_log | 1 个月 | 6 个月 | 12 个月 | 是 |

### 8.3 归档流程

```mermaid
sequenceDiagram
    participant S as 调度器
    participant A as 归档服务
    participant M as MySQL
    participant C as ClickHouse
    participant O as 对象存储
    
    Note over S,O: 月度归档任务
    S->>A: 触发归档 (每月 1 日)
    
    A->>M: 查询 N-3 月分区
    M-->>A: 返回数据
    
    A->>C: 写入 ClickHouse<br/>温数据层
    C-->>A: 写入确认
    
    alt 需要永久保存
        A->>O: 导出 Parquet 格式<br/>到对象存储
        O-->>A: 上传确认
    end
    
    A->>M: DROP 已归档分区
    A->>S: 归档完成
    
    Note over S,O: 年度冷数据清理
    S->>A: 触发清理 (每年 1 月)
    A->>O: 验证冷数据完整性
    A->>C: DROP 超期温数据
```

### 8.4 归档后查询方案

```mermaid
graph TB
    Q[用户查询] --> R[查询路由服务]
    
    R --> T{时间范围判断}
    
    T -->|最近 3 月 | M[MySQL 热数据]
    T -->|3-12 月 | C[ClickHouse 温数据]
    T -->|12 月以上 | O[对象存储冷数据]
    
    M --> D[返回结果]
    C --> D
    
    O --> E[触发离线查询<br/>Spark/Presto]
    E --> F[异步通知用户<br/>查询完成]
    F --> D
    
    style M fill:#d4edda
    style C fill:#fff3cd
    style O fill:#f8d7da
```

---

## 九、实施计划

### 9.1 阶段划分

```mermaid
gantt
    title 数据库分片与同步实施计划
    dateFormat  YYYY-MM-DD
    section 第一阶段 (2 周)
    分片表结构设计 :2026-04-10, 3d
    分片路由层开发 :2026-04-13, 7d
    历史数据迁移 :2026-04-20, 4d
    
    section 第二阶段 (2 周)
    Flink CDC 部署 :2026-04-24, 3d
    Kafka 集群搭建 :2026-04-27, 3d
    ClickHouse 表创建 :2026-04-30, 4d
    
    section 第三阶段 (2 周)
    数据同步联调 :2026-05-04, 5d
    一致性校验开发 :2026-05-11, 5d
    
    section 第四阶段 (1 周)
    归档服务开发 :2026-05-18, 5d
    生产环境部署 :2026-05-25, 3d
```

### 9.2 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 数据迁移期间服务中断 | 高 | 双写过渡，灰度切换 |
| CDC 同步延迟过高 | 中 | 增加 Kafka 分区，优化 Batch 大小 |
| 分片键设计不合理 | 高 | 预留二次分片能力 |
| ClickHouse 写入瓶颈 | 中 | 分布式表 + 多副本 |

---

## 十、监控指标

### 10.1 核心监控指标

| 指标 | 目标值 | 告警阈值 |
|------|--------|----------|
| CDC 同步延迟 | < 5 秒 | > 30 秒 |
| 分片数据均衡度 | > 90% | < 70% |
| 查询响应时间 (P99) | < 200ms | > 1s |
| 归档任务成功率 | 100% | < 99% |
| 数据一致性校验通过率 | 100% | < 99.9% |

---

## 附录：术语表

| 术语 | 解释 |
|------|------|
| CDC | Change Data Capture，变更数据捕获 |
| Binlog | MySQL 二进制日志，记录所有数据变更 |
| Shard Key | 分片键，用于确定数据归属的分片 |
| MergeTree | ClickHouse 表引擎家族 |
| TTL | Time To Live，数据生存周期 |

---

_文档版本：v1.0_  
_创建日期：2026-04-10_  
_下次评审：2026-07-10_

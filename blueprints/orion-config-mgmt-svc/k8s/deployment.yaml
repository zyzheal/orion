apiVersion: apps/v1
kind: Deployment
metadata:
  name: orion-config-mgmt-svc
  namespace: orion
  labels:
    app: orion-config-mgmt-svc
    version: v1
    component: config-mgmt-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orion-config-mgmt-svc
  template:
    metadata:
      labels:
        app: orion-config-mgmt-svc
        version: v1
        component: config-mgmt-service
    spec:
      containers:
        - name: config-mgmt-svc
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          image: orion-config-mgmt-svc:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3024
              name: http
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3024"
            - name: HOST
              value: "0.0.0.0"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: orion-config-mgmt-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: orion-config-mgmt-config
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: orion-config-mgmt-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: orion-config-mgmt-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: orion-config-mgmt-secrets
                  key: DB_PASSWORD
            - name: LOG_LEVEL
              value: "info"
            - name: CORS_ORIGIN
              valueFrom:
                configMapKeyRef:
                  name: orion-config-mgmt-config
                  key: CORS_ORIGIN
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3024
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3024
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      restartPolicy: Always

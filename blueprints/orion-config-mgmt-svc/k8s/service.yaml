apiVersion: v1
kind: Service
metadata:
  name: orion-config-mgmt-svc
  namespace: orion
  labels:
    app: orion-config-mgmt-svc
    component: config-mgmt-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3024
      protocol: TCP
  selector:
    app: orion-config-mgmt-svc
---
apiVersion: v1
kind: Service
metadata:
  name: orion-config-mgmt-svc-nodeport
  namespace: orion
  labels:
    app: orion-config-mgmt-svc
    component: config-mgmt-service
spec:
  type: NodePort
  ports:
    - name: http
      port: 3024
      targetPort: 3024
      nodePort: 30244
      protocol: TCP
  selector:
    app: orion-config-mgmt-svc
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: orion-config-mgmt-svc-hpa
  namespace: orion
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: orion-config-mgmt-svc
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
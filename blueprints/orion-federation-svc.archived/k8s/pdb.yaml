apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orion-federation-pdb
  namespace: orion
  labels:
    app: orion-federation
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: orion-federation

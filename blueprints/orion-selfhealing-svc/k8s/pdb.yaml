apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orion-selfhealing-pdb
  namespace: orion
  labels:
    app: orion-selfhealing
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: orion-selfhealing

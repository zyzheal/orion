apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orion-audit-pdb
  namespace: orion
  labels:
    app: orion-audit
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: orion-audit

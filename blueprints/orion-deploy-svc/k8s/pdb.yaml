apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orion-deploy-pdb
  namespace: orion
  labels:
    app: orion-deploy
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: orion-deploy

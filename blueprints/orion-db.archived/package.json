{
  "name": "orion-db-client",
  "version": "1.0.0",
  "description": "Orion Database Client - PostgreSQL and Redis connection utilities",
  "main": "src/index.js",
  "scripts": {
    "test:rls": "node src/test-rls.js",
    "test:redis": "node src/test-redis.js",
    "test:partitions": "node src/test-partitions.js",
    "test:all": "npm run test:rls && npm run test:redis && npm run test:partitions",
    "init": "node src/init-db.js"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "ioredis": "^5.3.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

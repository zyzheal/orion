apiVersion: apps/v1
kind: Deployment
metadata:
  name: orion-ticket-svc
  namespace: orion
  labels:
    app: orion-ticket-svc
    version: v1
    component: ticket-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orion-ticket-svc
  template:
    metadata:
      labels:
        app: orion-ticket-svc
        version: v1
        component: ticket-service
    spec:
      containers:
        - name: ticket-svc
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          image: orion-ticket-svc:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3004
              name: http
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3004"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: orion-ticket-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: orion-ticket-config
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: orion-ticket-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: orion-ticket-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: orion-ticket-secrets
                  key: DB_PASSWORD
            - name: DB_POOL_SIZE
              value: "10"
            - name: LOG_LEVEL
              value: "info"
            - name: CORS_ORIGIN
              valueFrom:
                configMapKeyRef:
                  name: orion-ticket-config
                  key: CORS_ORIGIN
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: orion-ticket-config
                  key: REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: orion-ticket-config
                  key: REDIS_PORT
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      restartPolicy: Always

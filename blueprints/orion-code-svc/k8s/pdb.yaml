apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orion-code-pdb
  namespace: orion
  labels:
    app: orion-code
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: orion-code

/**
 * Backup service re-export for backwards compatibility with backup routes.
 */
export { BackupService, BackupServiceError } from './BackupService';
export type { BackupEventType, BackupServiceOptions } from './BackupService';

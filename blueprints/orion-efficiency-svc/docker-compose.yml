version: '3.8'

services:
  efficiency-svc:
    build: .
    ports:
      - "3015:3015"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://orion:orion123@postgres:5432/efficiency_db
    depends_on:
      - postgres
    networks:
      - orion-network

  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=efficiency_db
      - POSTGRES_USER=orion
      - POSTGRES_PASSWORD=orion123
    ports:
      - "5432:5432"
    volumes:
      - efficiency-svc-data:/var/lib/postgresql/data
    networks:
      - orion-network

networks:
  orion-network:
    driver: bridge

volumes:
  efficiency-svc-data:

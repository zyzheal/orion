version: "3.8"
services:
  user-svc:
    build: .
    ports:
      - "8084:8084"
    environment:
      - DATABASE_URL=postgres://orion:orion@postgres:5432/orion_user?sslmode=disable
      - REDIS_ADDR=redis:6379
      - JWT_SECRET=change-me-in-production
      - SERVICE_NAME=orion-user-svc
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - orion
  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=orion
      - POSTGRES_PASSWORD=orion
      - POSTGRES_DB=orion_user
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orion"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - orion
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - orion
volumes:
  pgdata:
networks:
  orion:
    driver: bridge

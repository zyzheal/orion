apiVersion: v1
kind: Service
metadata:
  name: orion-monitor-svc
  namespace: orion
  labels:
    app: orion-monitor-svc
    component: monitor-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3005
      protocol: TCP
  selector:
    app: orion-monitor-svc
---
apiVersion: v1
kind: Service
metadata:
  name: orion-monitor-svc-nodeport
  namespace: orion
  labels:
    app: orion-monitor-svc
    component: monitor-service
spec:
  type: NodePort
  ports:
    - name: http
      port: 3005
      targetPort: 3005
      nodePort: 30055
      protocol: TCP
  selector:
    app: orion-monitor-svc
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: orion-monitor-svc-hpa
  namespace: orion
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: orion-monitor-svc
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
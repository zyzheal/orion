version: '3.8'

services:
  dr-svc:
    build: .
    ports:
      - "3016:3016"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://orion:orion123@postgres:5432/dr_db
    depends_on:
      - postgres
    networks:
      - orion-network

  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=dr_db
      - POSTGRES_USER=orion
      - POSTGRES_PASSWORD=orion123
    ports:
      - "5432:5432"
    volumes:
      - dr-svc-data:/var/lib/postgresql/data
    networks:
      - orion-network

networks:
  orion-network:
    driver: bridge

volumes:
  dr-svc-data:

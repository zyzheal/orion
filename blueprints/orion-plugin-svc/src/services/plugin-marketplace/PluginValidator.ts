/**
 * Stub: Plugin Validator
 * Validates plugin manifests and metadata.
 */

export class PluginValidator {
  validatePlugin(data: any): { valid: boolean; errors: string[] } {
    return { valid: true, errors: [] };
  }
}

version: '3.8'

services:
  plugin-svc:
    build: .
    ports:
      - "3011:3011"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://orion:orion123@postgres:5432/plugin_db
    depends_on:
      - postgres
    networks:
      - orion-network

  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=plugin_db
      - POSTGRES_USER=orion
      - POSTGRES_PASSWORD=orion123
    ports:
      - "5432:5432"
    volumes:
      - plugin-svc-data:/var/lib/postgresql/data
    networks:
      - orion-network

networks:
  orion-network:
    driver: bridge

volumes:
  plugin-svc-data:

# Orion Platform Migration Progress

## Current Session (2026-05-08)

### Completed: GAP-CN-01 Visual DAG Editor Backend API

实现了 Pipeline 可视化编排后端 API，支持 YAML 与 JSON 图格式的双向转换，以及全面的 YAML 校验。

#### Features Implemented
- **Created** `src/services/pipeline/PipelineGraphBuilder.ts`
  - buildGraph(pipelineId, yamlDefinition): 从已保存 pipeline 构建 DAG 图
  - buildGraphFromYaml(yamlString): 从 YAML 构建图（无需保存）
  - buildGraphFromSpec(): 从解析后的 spec 对象构建图
  - 自动计算拓扑排序布局位置（topological layering）
  - 支持 stage 节点和 task（step）节点
  - 支持扁平 YAML 和标准 apiVersion/kind/metadata/spec 格式

- **Created** `src/services/pipeline/YamlConverter.ts`
  - yamlToJson(): YAML -> JSON graph 转换 + 校验
  - jsonToYaml(): JSON graph -> YAML 转换
  - 双向转换往返测试通过

- **Created** `src/services/pipeline/PipelineValidator.ts`
  - validate(): 全面 YAML 校验
  - 环依赖检测（DFS 算法）
  - 缺失 dependsOn 目标检测
  - 重复 stage 名称检测
  - 无效 stage 类型/超时/重试次数检测
  - 不安全条件表达式检测（eval, require, process 等）

- **Created** `src/api/pipeline-graph-routes.ts`
  - GET  /api/v1/pipelines/:id/graph  - 从已保存 pipeline 构建图
  - POST /api/v1/pipelines/parse-yaml - YAML 转 JSON graph
  - POST /api/v1/pipelines/to-yaml   - JSON graph 转 YAML
  - POST /api/v1/pipelines/validate  - 校验 YAML spec

- **Updated** `src/api/pipeline-routes-registrar.ts` - 注册 graph 路由
- **Updated** `src/api/routes.ts` - 传递 pipelineService 到路由注册器

### Test Status
- **GAP-CN-01 tests**: 69 passed across 3 test suites
  - PipelineGraphBuilder: 21 tests
  - PipelineValidator: 27 tests
  - YamlConverter: 21 tests
- **All pipeline tests**: No new regressions

### Commits
- `b4aef85` feat(pipeline): implement GAP-CN-01 visual DAG editor backend API

### Files Changed
1. `src/services/pipeline/PipelineGraphBuilder.ts` (new)
2. `src/services/pipeline/PipelineValidator.ts` (new)
3. `src/services/pipeline/YamlConverter.ts` (new)
4. `src/api/pipeline-graph-routes.ts` (new)
5. `src/api/pipeline-routes-registrar.ts` (updated)
6. `src/api/routes.ts` (updated)
7. `src/services/pipeline/__tests__/PipelineGraphBuilder.test.ts` (new, 21 tests)
8. `src/services/pipeline/__tests__/PipelineValidator.test.ts` (new, 27 tests)
9. `src/services/pipeline/__tests__/YamlConverter.test.ts` (new, 21 tests)

### Completed: GAP-06 Re-run from Specific Stage

实现了 Pipeline 从指定阶段重跑的功能，支持从指定阶段开始或仅重跑失败阶段。

#### Features Implemented
- **Updated** `src/services/pipeline/PipelineService.ts`
  - retryRun() 新增 options 参数: `{ fromStage?: string; onlyFailed?: boolean }`
  - 自动计算 skippedStages（需要跳过的已完成阶段）
  - 自动计算 failedStages（仅 onlyFailed 时需要重跑的失败阶段）
  - 在 config_snapshot 中保存重试元数据供引擎消费
  - 返回值从 PipelineRun 改为 string（新 run 的 ID）

- **Updated** `src/engine/PipelineEngine.ts`
  - 新增 applyRetrySkipMetadata() 方法
  - execute() 中，初始化执行上下文后调用该方法
  - 预标记 skippedStages 中的阶段为 SUCCESS
  - 将预标记阶段从 pendingStages 移至 completedStages
  - 引擎正常流程自动跳过已完成阶段

- **Updated** `src/api/controllers/PipelineRunController.ts`
  - 新增 retry() 方法
  - 支持查询参数: ?fromStage=deploy&onlyFailed=true
  - 租户隔离和错误处理

- **Updated** `src/api/pipeline-routes-registrar.ts`
  - 注册 POST /v1/pipeline-runs/:id/retry 路由

- **Created** `src/services/pipeline/__tests__/PipelineService.rerun.test.ts` (15 tests)
  - 默认重跑行为（向后兼容）
  - 从指定阶段重跑（fromStage）
  - 仅重跑失败阶段（onlyFailed）
  - 组合使用 fromStage + onlyFailed
  - 无效状态检查（running/success 不可重跑）
  - 边界情况（无阶段执行记录、阶段不存在等）

### Test Status
- **GAP-06 tests**: 15 passed
- **All pipeline tests**: No regressions

### Commits
- `bbcd988` feat(engine): implement GAP-06 re-run from specific stage support

### Files Changed
1. `src/services/pipeline/PipelineService.ts` (retryRun enhancement)
2. `src/services/pipeline/__tests__/PipelineService.rerun.test.ts` (new, 15 tests)
3. `src/engine/PipelineEngine.ts` (applyRetrySkipMetadata method)
4. `src/api/controllers/PipelineRunController.ts` (retry method)
5. `src/api/pipeline-routes-registrar.ts` (retry route)

### Completed: GAP-10 External Notification Webhook (Connect HookChain to Pipeline Events)

实现了 Pipeline 出站 Webhook 通知功能，将 Pipeline 完成/失败/取消事件推送到外部系统。

#### Features Implemented
- **Fixed** `src/services/pipeline/WebhookNotifier.ts`
  - 修复了 shouldSend 方法中空事件数组的行为（空数组=不发送，undefined=发送所有）
  - WebhookNotifier 主类已存在，包含：
    - sendWebhook(): 指数退避重试（默认 3 次），HMAC-SHA256 签名
    - sendAll(): 批量并行发送，互不影响
    - shouldSend(): 事件过滤
    - generateHmacSignature(): HMAC-SHA256 签名生成

- **Created** `src/repositories/WebhookConfigRepository.ts`
  - findByPipelineId(): 按 Pipeline ID 查询所有启用的配置
  - findByEvent(): 按事件类型过滤查询
  - deleteByPipelineId(): 按 Pipeline ID 批量删除
  - create/update/delete: 标准 CRUD 操作

- **Created** `src/db/migrations/136_create_pipeline_webhook_configs.sql`
  - pipeline_webhook_configs 表：pipeline_id, url, method, headers, secret, events, enabled, retries
  - events 数组存储，空数组表示匹配所有事件

- **Updated** `src/engine/PipelineEngine.ts`
  - 新增 webhookNotifier 和 webhookConfigRepo 依赖注入（可选）
  - checkRunCompletion() 中，pipeline 完成后自动发送 Webhook 通知
  - sendWebhookNotifications() 私有方法：
    - 根据 Pipeline 最终状态映射事件类型（pipeline.complete/pipeline.failed/pipeline.cancelled）
    - 从仓库查询匹配的 Webhook 配置
    - 构建 payload（runId, pipelineId, status, durationMs, stagesSummary, triggerBy, metadata）
    - 并行发送所有匹配的 Webhook
  - buildStagesSummary() 辅助方法：从内存执行上下文或数据库获取 stages 摘要

- **Updated** `src/api/routes.ts`
  - 初始化 WebhookNotifier 和 WebhookConfigRepository
  - 传递到 PipelineEngine 构造函数

- **Created** `src/repositories/__tests__/WebhookConfigRepository.test.ts` (5 tests)
  - findByPipelineId, findByEvent, deleteByPipelineId, mapRowToEntity

- **Fixed** `src/services/pipeline/__tests__/WebhookNotifier.test.ts` 中的 1 个测试
  - shouldSend 空事件数组行为修复后，测试从 26 passed + 1 failed 变为 27 passed

### Test Status
- **WebhookNotifier tests**: 27 passed
- **WebhookConfigRepository tests**: 5 passed
- **IM notification tests**: 14 passed
- **Total notification tests**: 46 passed across 3 test suites

### Commits
- feat(notifications): implement GAP-10 external notification webhook integration

### Files Changed
1. `src/services/pipeline/WebhookNotifier.ts` (fixed empty events behavior)
2. `src/repositories/WebhookConfigRepository.ts` (new)
3. `src/repositories/__tests__/WebhookConfigRepository.test.ts` (new, 5 tests)
4. `src/db/migrations/136_create_pipeline_webhook_configs.sql` (new)
5. `src/engine/PipelineEngine.ts` (webhook notifier integration)
6. `src/api/routes.ts` (webhook initialization and wiring)

### Completed: GAP-02 Matrix Build Support

实现了 Pipeline Matrix 构建功能，类似 GitHub Actions 的 `strategy.matrix` 和 GitLab CI 的 `parallel:matrix`。

#### Features Implemented
- **Updated** `src/models/Pipeline.ts`
  - PipelineStage 新增 `matrix` 字段: `{ [key: string]: string[]; exclude?: Array<{[key: string]: string}> }`
  - PipelineStage 新增 `env` 字段: `Record<string, string>`

- **Created** `src/engine/MatrixExpander.ts`
  - MatrixExpander 工具类，处理矩阵扩展逻辑
  - hasMatrix(): 检测 stage 是否有矩阵配置
  - expandMatrix(): 展开单个 stage 的矩阵（笛卡尔积 - 排除项）
  - expandAll(): 展开所有 stages，自动重写依赖关系
  - getMatrixDimensions(): 计算矩阵维度总数
  - 命名格式: `stage-name (key1=value1, key2=value2)`
  - 环境变量注入: `MATRIX_KEY=VALUE` 格式
  - 依赖重写: 依赖矩阵 stage 的 stage 自动依赖所有展开实例
  - 支持部分排除匹配 (partial exclusion matching)

- **Updated** `src/engine/PipelineEngine.ts`
  - 新增 MatrixExpander import
  - execute() 方法中，YAML 解析后新增矩阵扩展步骤 (step 2.5)
  - 新增 initializeStagesFromExpanded() 方法处理展开后的 stages
  - Tasks 初始化也使用展开后的 stages
  - 扩展时记录日志（原始数量、扩展后数量、矩阵 stage 数量）

- **Created** `src/engine/__tests__/MatrixExpander.test.ts` (25 tests)
  - hasMatrix: 3 tests
  - 笛卡尔积: 4 tests (单维度、双维度、三维度、变量注入)
  - 排除项: 2 tests (指定组合排除、部分排除匹配)
  - 命名: 3 tests
  - Stage 属性保留: 6 tests
  - 依赖重写: 3 tests
  - 无矩阵 stage: 1 test
  - getMatrixDimensions: 3 tests

### Test Status
- **MatrixExpander tests**: 25 passed
- **Core engine tests**: 123 passed (MatrixExpander + PipelineCheckpointManager + ExpressionEvaluator + DebugController)
- **All pipeline tests**: No regressions

### Commits
- feat(engine): implement GAP-02 matrix build support

### Files Changed
1. `src/models/Pipeline.ts` (matrix + env fields)
2. `src/engine/MatrixExpander.ts` (new)
3. `src/engine/__tests__/MatrixExpander.test.ts` (new, 25 tests)
4. `src/engine/PipelineEngine.ts` (matrix integration)

### Completed: GAP-CN-05 IM Notification Integration

实现了钉钉、企业微信、飞书的 Pipeline 状态通知功能。

#### Features Implemented
- **Created** `src/services/pipeline/IMNotifier.ts`
  - IMNotifier 主类，使用适配器模式支持多种 IM 平台
  - IMNotificationConfig / IMNotificationPayload 接口定义
  - notifyOnPipelineComplete / notifyOnPipelineFailure / notifyOnPipelineCancelled 便捷方法
  - sendBatch 批量发送（并行，互不影响）
  - 所有通知发送失败被捕获并记录日志，不影响 Pipeline 状态

- **Created** `src/services/pipeline/im-adapters/DingTalkAdapter.ts`
  - 钉钉自定义机器人 Webhook 适配器
  - 发送 Markdown 格式消息，包含状态 emoji
  - 验证 HTTP 响应状态码和 errcode

- **Created** `src/services/pipeline/im-adapters/WeComAdapter.ts`
  - 企业微信群机器人 Webhook 适配器
  - 发送 Markdown 格式消息，使用 blockquote 和 font color
  - 成功/失败/取消分别使用 info/warning/comment 颜色

- **Created** `src/services/pipeline/im-adapters/FeishuAdapter.ts`
  - 飞书自定义机器人 Webhook 适配器
  - 发送交互式卡片消息，header 模板根据状态变色（green/red/gray）
  - 包含"查看详情"操作按钮

- **Updated** `src/engine/PipelineEngine.ts`
  - 新增 IMNotifier 和 IMNotificationConfig 构造参数（可选）
  - checkRunCompletion() 中，pipeline 完成后自动发送 IM 通知
  - sendIMNotifications() 私有方法，根据状态分发到不同通知方法
  - 通知发送使用 .catch() 包裹，确保失败不影响 pipeline 状态

- **Created** `src/services/pipeline/__tests__/IMNotifier.test.ts` (23 tests)
  - 适配器注册、通知分发、错误处理
  - PipelineComplete/Failure/Cancelled 便捷方法
  - sendBatch 批量发送和容错
  - Pipeline 状态保护（通知失败不抛异常）

- **Created** `src/services/pipeline/__tests__/im-adapters/DingTalkAdapter.test.ts` (10 tests)
- **Created** `src/services/pipeline/__tests__/im-adapters/WeComAdapter.test.ts` (10 tests)
- **Created** `src/services/pipeline/__tests__/im-adapters/FeishuAdapter.test.ts` (10 tests)
  - 每个 adapter 测试：平台类型、消息格式、状态变体、错误处理

### Test Status
- **IM notification tests**: 44 passed across 4 test suites
- **All pipeline tests**: 450+ passed (no regressions)

### Commits
- `13ee38d` feat(engine): implement GAP-CN-05 IM notification integration (DingTalk/WeCom/Feishu)

### Files Changed
1. `src/services/pipeline/IMNotifier.ts` (new)
2. `src/services/pipeline/im-adapters/DingTalkAdapter.ts` (new)
3. `src/services/pipeline/im-adapters/WeComAdapter.ts` (new)
4. `src/services/pipeline/im-adapters/FeishuAdapter.ts` (new)
5. `src/services/pipeline/im-adapters/index.ts` (new)
6. `src/services/pipeline/__tests__/IMNotifier.test.ts` (new, 23 tests)
7. `src/services/pipeline/__tests__/im-adapters/DingTalkAdapter.test.ts` (new, 10 tests)
8. `src/services/pipeline/__tests__/im-adapters/WeComAdapter.test.ts` (new, 10 tests)
9. `src/services/pipeline/__tests__/im-adapters/FeishuAdapter.test.ts` (new, 10 tests)
10. `src/engine/PipelineEngine.ts` (IM notifier integration)

### Completed: GAP-05 Execution State Persistence with Checkpoint Manager

将 Pipeline 执行状态从内存 Map 持久化到 PostgreSQL，支持 startup recovery。

#### Features Implemented
- **Created** `src/db/migrations/133_create_pipeline_checkpoints_table.sql`
  - pipeline_checkpoints 表：run_id UNIQUE, checkpoint_data JSONB, status 索引
  - Rollback migration: 133_rollback_pipeline_checkpoints_table.sql

- **Created** `src/repositories/PipelineCheckpointRepository.ts`
  - saveCheckpoint: INSERT ... ON CONFLICT (run_id) DO UPDATE upsert
  - findByRunId / findAllByStatus / deleteByRunId / findByPipelineId / countByStatus

- **Created** `src/engine/PipelineCheckpointManager.ts`
  - saveCheckpoint: 序列化 PipelineExecution 为 CheckpointData JSON
  - loadCheckpoint: 反序列化并恢复 PipelineExecution
  - cleanupCompleted: pipeline 完成后清理 checkpoint
  - findRunningCheckpoints: 查找所有 RUNNING 状态的 checkpoint
  - recoverOrphanedRuns: startup recovery - 恢复或标记失败
  - CheckpointData 包含: run, stages (含所有字段), pendingStages, runningStages, completedStages

- **Updated** `src/engine/PipelineEngine.ts`
  - 新增 checkpointManager 依赖注入
  - executeStage(): stage RUNNING/SUCCESS/FAILED 时自动 saveCheckpoint
  - Task 完成时 saveCheckpoint (记录 lastStageName, lastTaskName)
  - checkRunCompletion(): 成功后 cleanupCompleted
  - cancelExecution(): 取消时 cleanupCompleted
  - failDependentStages(): 跳过依赖 stage 时 saveCheckpoint
  - retryStage/retryStageWithBackoff(): retry 时 saveCheckpoint
  - recoverOrphanedRuns(): 新的 startup recovery 方法，优先使用 checkpoint
  - recoverRuns(): 兼容旧逻辑，fallback 时使用

- **Created** `src/repositories/__tests__/PipelineCheckpointRepository.test.ts` (13 tests)
  - saveCheckpoint 插入/更新, findByRunId, findAllByStatus, deleteByRunId
  - findByPipelineId, countByStatus

- **Created** `src/engine/__tests__/PipelineCheckpointManager.test.ts` (20 tests)
  - saveCheckpoint/loadCheckpoint/cleanupCompleted
  - findRunningCheckpoints
  - recoverOrphanedRuns: 恢复、标记失败、清理已完成的 checkpoint
  - Serialization round-trip 测试

### Test Status
- **Checkpoint tests**: 33 passed across 2 test suites
- **All pipeline tests**: 30 out of 32 suites pass (2 pre-existing failures unrelated)
- **Type check**: No new errors introduced

### Commits
- `feat(engine): implement GAP-05 execution state persistence with checkpoint manager`

### Files Changed
1. `src/db/migrations/133_create_pipeline_checkpoints_table.sql` (new)
2. `src/db/migrations/133_rollback_pipeline_checkpoints_table.sql` (new)
3. `src/repositories/PipelineCheckpointRepository.ts` (new)
4. `src/repositories/__tests__/PipelineCheckpointRepository.test.ts` (new, 13 tests)
5. `src/engine/PipelineCheckpointManager.ts` (new)
6. `src/engine/__tests__/PipelineCheckpointManager.test.ts` (new, 20 tests)
7. `src/engine/PipelineEngine.ts` (checkpoint integration)

### Completed: GAP-01 Condition Expression Engine Enhancement

Replaced simple regex-based condition evaluation in PipelineEngine with a full expression engine.

#### Features Implemented
- **Created** `src/engine/ExpressionEvaluator.ts`
  - Uses `expr-eval` library with strict security whitelisting
  - Comparison operators: ==, !=, >, <, >=, <=
  - Logical operators: &&, ||, ! (converted to expr-eval's and/or/not)
  - String functions: startsWith(), endsWith(), contains() (works with both strings and arrays)
  - Status functions: success(), failure(), cancelled(), always()
  - Context variables: branch, tags, changedFiles, triggerBy
  - Timeout protection (100ms max)
  - Security: blocks eval, Function, require, process, __proto__, constructor, prototype, etc.
  - Graceful handling of undefined variables

- **Updated** `src/engine/PipelineEngine.ts`
  - Added ExpressionEvaluator as class member
  - Replaced evaluateCondition() with full expression evaluation
  - Added mapRunStatusToExpression() for status function support
  - Error logging with fallback to false on evaluation failure

- **Created** `src/engine/__tests__/ExpressionEvaluator.test.ts` (45 tests)
  - Comparison operators (8 tests)
  - Logical operators (6 tests)
  - String functions (4 tests)
  - Status functions (5 tests)
  - Context variables (4 tests)
  - Security blocked functions (6 tests)
  - Edge cases (6 tests)
  - Timeout protection (1 test)
  - Real-world pipeline scenarios (5 tests)

#### Dependencies Added
- `expr-eval@^2.0.2` - Expression evaluation library
- `@types/expr-eval` - TypeScript types

### Commit
- `80439cd` feat(engine): implement condition expression engine for pipeline stages

### Previous: Pipeline Plugin System Phase 4 — Security Hardening

All 5 features of Phase 4 implemented and tested:

#### Feature 1: Pipeline Tenant Isolation (Security Fix)
- **Created** `src/services/pipeline/PipelineTenantIsolationService.ts`
  - Extract tenantId from x-tenant-id header
  - validatePipelineTenant: ensures pipeline belongs to requesting tenant
  - validateRunTenant: ensures run belongs to requesting tenant
- **Updated** `src/api/controllers/PipelineRunController.ts` — all endpoints now validate tenant:
  - trigger(): validates pipeline tenant before execution
  - cancel(): validates run tenant before cancellation
  - list(): filters runs by tenant scope
  - getById/getStages/getTasks(): all validate run tenant access
- **Fixed** `src/services/pipeline/PipelineRunService.ts` — mapCreateInput now extracts tenantId from context instead of hardcoding default tenant
- Returns 403 TENANT_ISOLATION_VIOLATION on cross-tenant access attempts

#### Feature 2: Container Image Signature Verification
- **Created** `src/services/plugin/ImageVerifier.ts`
  - verifyImage: full image verification with digest pinning support
  - Warns when using :latest tag (non-deterministic)
  - Digest-pinned images (@sha256:...) use as-is without warnings
  - docker inspect to verify image exists locally
  - Pulls image based on pull policy (Always/IfNotPresent/Never)
  - Registry auth support via DOCKER_REGISTRY_USERNAME/PASSWORD env vars
  - Uses --password-stdin for credential safety
- **New tests**: `src/services/plugin/__tests__/ImageVerifier.test.ts` (13 tests)

#### Feature 3: Pipeline-level RBAC
- **Created** `src/services/pipeline/PipelineRBACService.ts`
  - Roles: pipeline.admin, pipeline.editor, pipeline.viewer, pipeline.approver
  - canTrigger/canView/canCancel/canApprove permission checks
  - If no RBAC rules exist for a pipeline, default to allow (backward compatible)
  - In-memory rule store with setRules/addRule/removeRule operations
- **Wired into** PipelineRunController.trigger() and cancel()
- **New tests**: `src/services/pipeline/__tests__/PipelineRBACService.test.ts` (23 tests)

#### Feature 4: SCM Webhook Receiver
- **Created** `src/services/pipeline/SCMWebhookService.ts`
  - handleGitHubPush: parse GitHub push events with HMAC signature validation
  - handleGitLabPush: parse GitLab push events with secret token validation
  - Trigger rules matching: repository pattern, branch pattern, event type
  - Auto-triggers matching pipelines with SCM context
  - Stores webhook events (last 100) for history
- **Added route** POST /api/v1/webhooks/scm (public, signature validated)
- **Added route** GET /api/v1/webhooks/scm/events (auth protected)
- **New tests**: `src/services/pipeline/__tests__/SCMWebhookService.test.ts` (16 tests)

#### Feature 5: Container Image Pre-pull Enhancement
- ImageVerifier consolidates and enhances the existing pullImageIfNeeded logic
- Adds clear error messages when image pull fails
- Registry auth with DOCKER_REGISTRY_SERVER/USERNAME/PASSWORD env vars

### Test Status Summary
- **Pipeline-related tests**: 450 passed across 25 test suites
- **New tests this session**: 52 (13 + 23 + 16)
- **Total new tests across all sessions**: 407 (355 previous + 52 this session)

### Commits This Session (Phase 4)
1. `feat(security): implement Phase 4 security hardening for pipeline plugin system`

### Files Changed This Session (Phase 4)
1. `src/services/pipeline/PipelineTenantIsolationService.ts` (new)
2. `src/services/pipeline/PipelineRBACService.ts` (new)
3. `src/services/pipeline/SCMWebhookService.ts` (new)
4. `src/services/plugin/ImageVerifier.ts` (new)
5. `src/services/pipeline/__tests__/PipelineRBACService.test.ts` (new, 23 tests)
6. `src/services/pipeline/__tests__/SCMWebhookService.test.ts` (new, 16 tests)
7. `src/services/plugin/__tests__/ImageVerifier.test.ts` (new, 13 tests)
8. `src/api/controllers/PipelineRunController.ts` (tenant isolation + RBAC wiring)
9. `src/api/pipeline-routes-registrar.ts` (SCM webhook routes)
10. `src/services/pipeline/PipelineRunService.ts` (fix hardcoded tenantId)

### Previous: Pipeline Plugin System Phase 3 — Production Readiness (SRE Review)

All 5 features of Phase 2 implemented and tested:

#### Feature 1: Parallel Stage Execution + Fan-out/Fan-in
- **Modified** `src/engine/PipelineEngine.ts` executePendingStages() to detect independent stages
  and execute them concurrently using Promise.allSettled
- Added locking mechanism in checkNextStages to prevent concurrent calls from parallel completions
- Enhanced fan-in: dependent stages only unlock when ALL dependencies succeed
- Failed dependency causes dependent stages to be skipped automatically

#### Feature 2: Artifact Passing Between Stages
- **Created** `src/services/pipeline/ArtifactService.ts` with filesystem-based storage
- upload/download/listByRun/listByStage/passToStage operations
- Auto-passes artifacts when stage dependencies resolve in PipelineEngine
- StageExecutor gains getAvailableArtifacts and passUpstreamArtifacts methods
- **New tests**: `src/services/pipeline/__tests__/ArtifactService.test.ts` (18 tests)

#### Feature 3: Cron Scheduler
- **Updated** `src/services/pipeline/PipelineTriggerService.ts` with cron scheduling
- scheduleTrigger/unscheduleTrigger using CronExpressionParser for validation
- Auto-schedules when registering schedule-type triggers with cronExpression
- Callback-based execution: onTick fires pipeline runs and records history
- Reschedules on status changes (inactive -> active)
- **New tests**: `src/services/pipeline/__tests__/PipelineTriggerService.cron.test.ts` (14 tests)

#### Feature 4: Approval Gate
- **Created** `src/services/pipeline/ApprovalGateService.ts` with request/approve/reject lifecycle
- Enforces approver authorization: only configured approvers can approve/reject
- Wired into PipelineEngine: checks approval before executing stages
- Added API endpoints for approval management (list, get status, approve, reject)
- Extended ApprovalController with pipeline approval gate methods
- **New tests**: `src/services/pipeline/__tests__/ApprovalGateService.test.ts` (21 tests)

### Test Status Summary
- **Pipeline-related tests**: 94 passed across 5 test suites
- **Total project tests**: 6221+ passed (6257 total, 4 pre-existing failures unrelated to this session)
- **New tests this session**: 53 (18 + 14 + 21)
- **Total new tests across both sessions**: 355 (302 previous + 53 this session)

### Commits This Session (Phase 2)
1. `461ba6f` feat(engine): implement parallel stage execution with fan-out/fan-in
2. `d9e7b6d` feat(pipeline): add ArtifactService for stage-to-stage artifact passing
3. `75992d7` feat(pipeline): add cron scheduler using cron-parser
4. `ad7aa39` feat(pipeline): add approval gate for human approval before deployment

### Files Changed This Session (Phase 2)
1. `src/engine/PipelineEngine.ts` (parallel execution + approval gate + artifact passing)
2. `src/engine/StageExecutor.ts` (artifact awareness methods)
3. `src/services/pipeline/ArtifactService.ts` (new)
4. `src/services/pipeline/__tests__/ArtifactService.test.ts` (new, 18 tests)
5. `src/services/pipeline/PipelineTriggerService.ts` (cron scheduler)
6. `src/services/pipeline/__tests__/PipelineTriggerService.cron.test.ts` (new, 14 tests)
7. `src/services/pipeline/ApprovalGateService.ts` (new)
8. `src/services/pipeline/__tests__/ApprovalGateService.test.ts` (new, 21 tests)
9. `src/api/controllers/ApprovalController.ts` (pipeline approval endpoints)
10. `src/api/pipeline-routes-registrar.ts` (approval routes)

### Previous: Pipeline Plugin System Remaining Gaps (G1-G6)

All 5 phases of the Pipeline Plugin System gap implementation are complete.

#### Phase 1: WASM Runtime (G1 + G5)
- **Rewrote** `src/services/inline-script/WasmRuntime.ts` to use real QuickJS execution
- **Updated** `src/services/plugin-executor-service.ts` executeWASMPlugin() to delegate to WasmRuntime
- **New tests**: `src/services/inline-script/__tests__/WasmRuntime.test.ts` (12 tests)
- Verified: QuickJS isolates from Node.js APIs, enforces timeouts, captures console.log

#### Phase 2: AI Diagnosis (G2)
- **Rewrote** `src/services/ai/AIDiagnosisService.ts` with HTTP call to orion-ai-service
- Added 12 rule-based diagnosis patterns (network, memory, permissions, Docker, auth)
- Graceful fallback when AI service is unavailable
- **New tests**: `src/services/ai/__tests__/AIDiagnosisService.test.ts` (12 tests)

#### Phase 3: AI Script Generation (G3)
- **Created** `src/services/ai/AIGenerateService.ts` with 17 script templates
- **Wired** `/ai-generate` endpoint in `src/api/script-routes.ts` (replaced 501 stub)
- Template-based fallback with keyword matching
- **New tests**: `src/services/ai/__tests__/AIGenerateService.test.ts` (11 tests)

#### Phase 4: Debug Controller (G4)
- **Created** `src/engine/DebugController.ts` with pause/resume/step/getState
- **Wired** debug endpoints in `src/api/plugin-routes.ts` (replaced 3x 501 stubs)
- Added GET /:runId/debug/state endpoint
- Singleton pattern for global debug state access
- **New tests**: `src/engine/__tests__/DebugController.test.ts` (16 tests)

#### Phase 5: Container Image Validation (G6)
- Added `PullPolicy` enum (Always, IfNotPresent, Never)
- Added `pullImageIfNeeded()` with local image check and pull logic
- Added `ensureRegistryAuth()` with --password-stdin for credential safety
- Added digest pinning support and :latest tag warnings
- **New tests**: `src/services/__tests__/plugin-executor-image.test.ts` (7 tests)

### Test Status Summary
- **Test Suites**: 309 passed, 312 total (3 pre-existing Docker failures)
- **Tests**: 6180 passed, 6204 total (新增 58 个 from this session)
- **New test files**:
  - WasmRuntime.test.ts (12 tests)
  - AIDiagnosisService.test.ts (12 tests)
  - AIGenerateService.test.ts (11 tests)
  - DebugController.test.ts (16 tests)
  - plugin-executor-image.test.ts (7 tests)

### Commits This Session
1. `bd58653` Phase 1: Implement real WASM runtime via quickjs-emscripten (G1 + G5)
2. `fac8fc4` Phase 2: Implement AI Diagnosis with rule-based fallback (G2)
3. `1a18486` Phase 3: Implement AI Script Generation with template fallback (G3)
4. `ce43f3f` Phase 4: Implement Debug Controller with pause/resume/step (G4)
5. `469b6a5` Phase 5: Add container image validation and registry auth (G6)

### Files Changed This Session
1. `src/services/inline-script/WasmRuntime.ts` (rewrite - QuickJS execution)
2. `src/services/inline-script/__tests__/WasmRuntime.test.ts` (new, 12 tests)
3. `src/services/plugin-executor-service.ts` (WASM delegation + image validation)
4. `src/services/ai/AIDiagnosisService.ts` (rewrite - AI + rule-based fallback)
5. `src/services/ai/__tests__/AIDiagnosisService.test.ts` (new, 12 tests)
6. `src/services/ai/AIGenerateService.ts` (new)
7. `src/services/ai/__tests__/AIGenerateService.test.ts` (new, 11 tests)
8. `src/api/script-routes.ts` (wire /ai-generate endpoint)
9. `src/engine/DebugController.ts` (new)
10. `src/engine/__tests__/DebugController.test.ts` (new, 16 tests)
11. `src/api/plugin-routes.ts` (wire debug endpoints)
12. `src/services/__tests__/plugin-executor-image.test.ts` (new, 7 tests)
13. `jest.config.js` (transformIgnorePatterns for quickjs-emscripten)
14. `package.json` (added quickjs-emscripten dependency)

### Next Steps
1. P2 - 测试覆盖率门槛配置
2. P3 - CI/CD 改进
3. Consider integrating DebugController into PipelineEngine/StageExecutor for actual pause-at-boundary behavior

## Previous Sessions (2026-05-08 - Phase 3)

### Completed: Pipeline Plugin System Phase 3 — Production Readiness (SRE Review)

All 6 features of Phase 3 implemented and tested:

#### Feature 1: Global Execution Queue with Backpressure
- **Created** `src/services/pipeline/PipelineExecutionQueue.ts`
- Priority-based queue (HIGH/NORMAL/LOW) using sorted array
- Max concurrent runs configurable via MAX_CONCURRENT_PIPELINES env var (default 10)
- Backpressure: rejects new runs when queue exceeds MAX_QUEUE_SIZE (default 100)
- Wired into PipelineEngine.execute() — enqueues instead of immediate execution
- Added queue stats, queued runs listing, and cancel-from-queue APIs

#### Feature 2: Real TaskRunner Execution
- **Rewrote** `src/engine/TaskRunner.ts` executeGit/npm/k8s/shell methods
- git/*: Real `spawn('git', [...])` for clone/checkout/push/pull/status/log
- npm/*: Real `spawn('npm'/'npx', [...])` with timeout support
- shell/*: Real `spawn('sh', ['-c', script])` with dangerous command filtering
- k8s/*: Real `spawn('kubectl', [...])` for apply/delete/rollout/get/describe/logs
- All tasks: capture stdout/stderr, handle timeouts, return real exit codes
- Clean environment: minimal PATH, HOME, no secrets
- Graceful fallback to mock if required binary unavailable

#### Feature 3: Persistent Pipeline State & Crash Recovery
- **Added** `PipelineRunRepository.findByStatus()` for querying runs by status
- **Added** `PipelineRunService.findRunsByStatus()`
- `PipelineEngine.recoverRuns()`: finds RUNNING runs from DB, marks as failed
- Wired into `src/api/routes.ts` startup — runs recovery on each server start
- `rebuildExecutionQueue()`: placeholder for future persistent execution progress

#### Feature 4: EventBus Wiring (Verified — already complete)
- EventBusService properly wired to PipelineEventPublisher via EventBusAdapter
- NATS publish with PostgreSQL persistence, in-memory fallback, and retry on reconnect
- No changes needed — existing implementation is production-ready

#### Feature 5: Auto-Retry Integration
- **Added** `AutoRetryService` as constructor parameter to PipelineEngine
- Stage failure triggers `AutoRetryService.shouldRetry()` with error classification
- Exponential backoff with jitter for transient errors
- Fallback to simple retry logic when AutoRetryService unavailable
- New `retryStageWithBackoff()` method for intelligent retry scheduling

#### Feature 6: Metrics Export
- **Created** `src/services/pipeline/PipelineMetricsService.ts`
- Tracks: total runs, success rate, avg/median/p95 duration
- Failures by error type, runs by pipeline, runs by trigger type
- Queue depth monitoring
- `getPrometheusMetrics()`: exports standard Prometheus text format
- **New endpoints**:
  - `GET /v1/pipeline/metrics` — JSON metrics
  - `GET /v1/pipeline/metrics?format=prometheus` — Prometheus format
  - `GET /v1/pipeline/metrics/:pipelineId` — Per-pipeline metrics
  - `GET /v1/pipeline/queue` — Queue status and pending runs
- Wired via `onRunComplete` callback in PipelineEngine

### Test Results
- All 23 pipeline test suites pass (411+ tests)
- No new TypeScript type errors introduced
- Recovery mechanism verified: "No running pipeline runs to recover"

### Commit
13. `1358477` Implement Phase 3: Pipeline Plugin System production readiness features

### Files Changed This Session (Phase 3)
1. `src/services/pipeline/PipelineExecutionQueue.ts` (new)
2. `src/services/pipeline/PipelineMetricsService.ts` (new)
3. `src/engine/PipelineEngine.ts` (queue, retry, recovery, metrics callback)
4. `src/engine/TaskRunner.ts` (real task execution)
5. `src/services/pipeline/PipelineRunRepository.ts` (findByStatus)
6. `src/services/pipeline/PipelineRunService.ts` (findRunsByStatus)
7. `src/api/routes.ts` (wire all Phase 3 features + new endpoints)

## Previous Sessions (2026-05-07)

### Completed: P1 功能域测试完善
- Multi-Cloud, IaC, Data Pipeline 功能域测试 (96 new tests)

### Completed: Federation 功能域增强 (P1)
- 37 Federation tests (39 new tests)

### Frontend Mock Data Migration (Session 2)
- 10 frontend pages migrated from mock data to real API connections

## Key Achievements
- Pipeline Plugin System: ALL remaining gaps (G1-G6) implemented and tested
- 从 5902 增加到 6204，总共新增 302 个测试
- 0 regressions introduced
